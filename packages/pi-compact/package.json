{
  "name": "pi-compact",
  "version": "0.1.0",
  "description": "Opt-in compaction policy for pi: early trigger, cheap summarizer, /compact config.",
  "license": "MIT",
  "type": "module",
  "keywords": ["pi-package", "pi", "picode"],
  "files": [
    "extensions",
    "src",
    "compact.schema.json",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "check": "tsc",
    "test": "npm run check && node --test test/*.test.ts"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.85.0",
    "@earendil-works/pi-server": "0.85.0",
    "@types/node": "24.0.0",
    "typescript": "5.9.2"
  },
  "pi": {
    "extensions": ["./extensions/compact.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cfpperche/picode.git",
    "directory": "packages/pi-compact"
  }
}
