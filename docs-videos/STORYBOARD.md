# Storyboard — PiCode docs tutorials

Three standalone compositions. One Frame block per composition (dispatch unit);
each builds inline (≤5 scenes each, per the ≤6-scene inline rule).

Design truth: `BRIEF.md` (house tokens, parity rule). Canvas 1280x720@30.

---

## Frame 1 — create-an-agent

- **status:** outline
- **src:** `compositions/create-agent.html`
- **citation:** `multi-phase-camera` (per UI-scene phase zoom) + `viewport-change` (single-writer camera on the phone-free scenes); text beats use plain rise/fade from the minimal skeleton (power3.out)
- **beats:**
  1. `0.0–3.0` Title card: "Create an agent" / "You never touch a terminal." (accent underline draws)
  2. `3.0–7.0` Real dashboard still — camera focuses the **+ New agent** control (accent ring + zoom via coordinate-target-zoom math)
  3. `7.0–10.8` New-agent form still — slow push-in; caption "Name it, give it a job"
  4. `10.8–14.2` Agent card still (running) — caption "It runs in tmux — reload-safe"
  5. `14.2–17.0` Outro: "You stay in command." + `cfpperche.github.io/picode`
- **duration:** 17.0

## Frame 2 — automate-it

- **status:** outline
- **src:** `compositions/automate-it.html`
- **citation:** `multi-phase-camera` + `depth-of-field-blur` (dim non-focal regions of stills); text beats as Frame 1
- **beats:**
  1. `0.0–3.0` Title: "Automate anything" / "On a schedule, or on a webhook."
  2. `3.0–6.6` Automations list still — focus ring on a row
  3. `6.6–10.4` Detail still — caption "Hourly, daily, or a custom cron"
  4. `10.4–14.0` Inbox still — caption "Every run lands in your inbox"
  5. `14.0–17.0` Outro: "Runs while you sleep." + docs URL
- **duration:** 17.0

## Frame 3 — take-it-anywhere

- **status:** outline
- **src:** `compositions/take-it-anywhere.html`
- **citation:** `viewport-change` (single world; phone frame scales in) + `split-tilt-cards` entry spirit (single straight rise, no orbit)
- **beats:**
  1. `0.0–3.0` Title: "PiCode in your pocket"
  2. `3.0–6.8` Phone frame — mobile fleet still; caption "Same agents, same sessions"
  3. `6.8–10.2` Phone frame — mobile agent still; caption "Steer it from the bus"
  4. `10.2–13.0` Phone frame — inbox still; caption "Approve runs on the go"
  5. `13.0–15.5` Outro: "Same server. Same agents." + docs URL
- **duration:** 15.5
