// PiCode docs theme: the VitePress default theme wearing the app's tokens
// (web/src/styles/app.css). One generator, not ours (docs/benchmarks.md bar).
import DefaultTheme from "vitepress/theme";
import "./custom.css";

export default DefaultTheme;
