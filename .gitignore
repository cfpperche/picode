# Go
bin/
*.exe
*.test
coverage.out

# Frontend
# No trailing slash: `node_modules/` matches only directories, which is how a
# symlink named node_modules once got committed by `git add -A`.
node_modules
dist/
web/dist/
www/.vitepress/cache/
www/.vitepress/dist/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Git worktrees (AGENTS.md: isolated trees per agent)
.worktrees/

# Local
.env
.env.*
*.log
var/
