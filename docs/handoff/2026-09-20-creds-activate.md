# 2026-09-20 — feat/creds-activate: any saved account, in the CLI's own credential file

Shipped: **Use** on a credential row writes the chosen vault account into that CLI's own file — `internal/clicreds/render.go` renders one format per declared CLI (pi, opencode, claude, codex, grok, hermes, muse, agy), merging the account and preserving every key PiCode does not own, refusing what it cannot write faithfully (omp has no file; grok with no session; claude/hermes/agy api keys; muse oauth — each refusal names its reason). `POST /api/credentials/{provider}/{id}/activate {cli}` in `internal/server/credentials.go` refuses with 409 while a terminal of that CLI is live (naming the count), keeps the replaced file once at `<DataDir>/credfiles/<cli>-<unix>.bak`, writes atomically at 0600. `active` now means "the file this CLI reads holds this account" for guest CLIs (pi's roster keeps its own slot meaning); `singleOAuth` marks a login carrying no account name, so there is one subscription row per provider there and the pane says so in a line. Both apps: **Use** first in the row menu with a confirm, an "In use" chip on the held row. ADR-0166 generalizes the pi model — no home move, no config-dir variable — per `docs/plans/agent-cli-credentials.md`.
Verified: `make ci-scoped` PASS (fmt, vet, hooks, go, test-js, build, docs). `internal/clicreds/render_test.go` — 11 funcs / 48 subtests: round-trip per format, unknown keys preserved, refusals, mutation-checked. `internal/server/credentials_test.go` — activation end to end: two Codex logins kept, the file decides which is in use, the file's unknown key survives the write, exactly one backup, 409 "Close the 1 running Codex terminal" while a runtime is registered, refusals for omp and an undeclared provider. Visual review on a scratch instance: PASS, screenshots in `var/screenshots/` (`credact-menu`, `credact-confirm`, `credact-inuse`); `window.__picodeOverlayAudit()` ok:true in every state (menu 471→641, confirm 291→465, toast 24→63 inside a 900px viewport); the Use click wrote `~/.codex/auth.json` in the isolated scratch HOME, verified on disk by key names only (auth_mode chatgpt, tokens.access_token/refresh_token/account_id), no values read, and no backup dir when there was no file to replace. Blind spot: the review covers those captured scratch-instance states only.
visual-review: 1 overlay inside the screenshot? yes. 2 every item readable? yes. 3 trigger still usable? yes. 4 clip/double-scroll/dead hover? no. 5 terminal-averse next click obvious? yes.
Merge: fast-forward ready.

## Next up

- Guided vendor sign-in and harvest (read the refreshed token back), nameless-login identity from the vendor's profile endpoint at import, and env-var injection at launch — declared, not wired; `docs/plans/agent-cli-credentials.md`.

## Debts

- A vault row's copy goes stale once its CLI renews the token: no harvest yet, re-import by hand is the workaround; one live account per CLI at a time, switching machine-wide — `docs/decisions/0166-credential-activation.md`.
