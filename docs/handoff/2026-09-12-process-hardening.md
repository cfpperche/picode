# 2026-09-12 — feat/process-hardening: the board stops being a file everyone writes

Shipped: the five flow fixes agreed after measuring the parallel-agent loop
(ADR-0123, 0124, 0125). `docs/handoff.md` is generated by `make handoff` (git
state + `docs/handoff/open/<topic>.md` + session notes; 120 lines/12 KB budget
that fails loudly) and is git-ignored — the hook refuses a forced copy and
`hooks-check` covers it. The old board's content moved into 16 topic files,
prose compressed where it narrated; nothing dropped except the stale
`desktop-v2-mgmt` entry, which `make worktree-status` now reports from disk
(B). `make close` reuses a green run when the merge left the covered content
untouched, by per-path blob hash (`scripts/ci-scope-reuse.mjs`, 7 tests) (C).
`docs-site/public/llms.txt` left git, produced by `make docs`/`make deploy`
(D). `make dev`/`ci-scoped`/`close` print worktree + branch, and AGENTS.md
gained the `pkill -f` ban (E).

Verified: `node --test scripts/*.test.mjs` green (13 new tests);
`make hooks-check` — every guard row, including "committed board refused" and
"forced board commit refused"; `make ci-scoped` PASS; the reuse path exercised
by merging `main` before close.

visual-review: n/a (no UI surface changed).

Not done / debts (all in `docs/handoff/open/process.md`): hook edits cannot be
exercised from the worktree that makes them — an absolute `core.hooksPath`
makes every tree run the root's hooks, so `make hooks-check` is the local proof
and this branch needed `git -c core.hooksPath=$PWD/.githooks` for the one
commit the branch's own hook allows; the three stalled worktrees are reported,
not resolved (owner's call); the budget moved 8 KB → 12 KB.

Merge: fast-forward ready.
