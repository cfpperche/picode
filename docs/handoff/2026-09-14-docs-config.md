# 2026-09-14 — docs-config: Configure overview

Shipped: `/guide/configure` — two worlds, "If you only remember one
thing", what to open, change workflow, custom-endpoint example. Settings,
Providers, Model roles get Where / Not this. Sidebar Configure links here.

Verified: `make docs` + Vale. agent-browser :4178 — desktop light/dark,
phone 390×844 (callout + list; a 3-col table was dropped because it
clipped). Screenshots `var/screenshots/docs-config-*.png` (not committed).

visual-review: n/a (docs site).

Left: MCP cookbook.

Merge: fast-forward ready.
