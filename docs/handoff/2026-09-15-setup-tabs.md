# 2026-09-15 — setup-tabs: setup tabs for every launchable CLI

Task: after seeing Grok's Providers tab read 'Providers are not available for this CLI. Open Pi', the owner asked for the five setup tabs (Providers, Settings, Keyboard, Packages, Connectors) to appear for Muse Code and Antigravity too, and for the other CLIs' decorative tabs that mention Pi to say the functionality is in development and coming soon; no real editor until Muse/Antigravity launch is equivalent to the others (owner, 2026-09-15).
Shipped: cliPanes (web/shared/domain/cliLaunch.js) now returns Launch, Terminals, Sessions (when the source exists) plus the five setup tabs for every launchable CLI — the setup group was previously gated on integrationCapable, which is why Muse/Antigravity had three tabs. Each setup pane's !supported branch now renders '<Tab> for <CLI> are in development — coming soon' (CLI name from terminalCliLabel) and no longer links to Pi: CliProviders, CliSettings (also the keyboard wording), CliPackages and the connectors view in AgentClis.jsx, in web/browser and web/mobile. Pi keeps its real panes (its providers pane still shows Add provider / anthropic / deepseek). CliPaneTabs (both shells) scrolls the selected tab into view: with eight tabs a phone deep link to Packages read Launch…Sessions while the panel said Packages, which the visual review caught.
Verified: on a scratch, the rendered pane body for muse/agy/grok × the five tabs (all ten read 'in development — coming soon'); pi settings/keyboard/packages real, pi providers real. Desktop screenshots read for muse providers, muse keyboard, agy connectors, grok providers, pi providers, and mobile 390px for muse packages (active tab visible after the scroll fix). Found and fixed in the same session: the connectors placeholder called terminalCliLabel without importing it in both AgentClis.jsx files, which blanked the whole page on #/clis/<cli>/connectors — the browser sweep over every tab of four CLIs is what caught it.
Observation, not this change: pi's Connectors pane renders nothing in a workspace-less instance (Mcps without a workspace); the branch this change touched is unreachable for pi.
visual-review: PASS (var/screenshots/{muse-providers,muse-keyboard,agy-connectors,grok-providers,pi-providers,mobile-muse-packages}.png; card 5/5)
