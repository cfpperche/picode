# 2026-09-19 — the shot photographed our own UI (feat/annot-shot-clean)

Third Send: the crop finally arrived (624×64 — geometry correct: element
404.667×32 × scale 1.48 + padding, exactly what cropRect computes) and it was
a photograph of OUR card covering the breadcrumb. The overlay is DOM inside
the page, so it lands in every capture.

## What landed
- `hide`/`show` on the page handle + `HIDE_SCRIPT`/`SHOW_SCRIPT`; new shell
  command `btab_annotate_overlay(id, on)`, used by the crop around the
  capture (with `finally`, so the overlay always comes back).
- Artifacts for the command: handler, manifest, capability, autogenerated
  permission, regenerated schemas. Cross-build green, Rust 16/16.
- Harness proof of the door: the host's computed visibility goes
  visible → hidden (chip included) → visible.

## Next up
- Owner: desktop-restart (new shell command) + deploy, then one Send — the
  picture should show the element with no pin or chip in it.

## Debts
- The capture itself stays Windows-only; the door and the geometry are what
  this branch proves.
- Lesson re-paid: `pkill -f "<pattern>"` killed the shell that ran it (the
  pattern matched its own command line). Use `fuser -k <port>/tcp`.
