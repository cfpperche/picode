# 2026-09-15 — agy-activity: Antigravity activity reporting

Fatia 5 of docs/plans/launch-muse-agy.md: Antigravity activity reporting (owner approved 2026-09-15); muse stays Open, serve deferred to future managed agents per owner.
Mechanism: PiCode installs a reporter as the CLI's title command (settings.json merge; foreign blocks refused, malformed never clobbered, ours removed on toggle-off); reporter POSTs agent_state through picode-hook (mapper learned the agent_state dialect, stale IDE transcript_path dropped) and prints the short title. Measured live: idle/thinking/working/tool_use/initializing map correctly; a broken reporter does not impair turns (proven with hook path pointed at /bin/false). No hooks.json decision hook ships (it gates tools).
Caught by gates, twice: the store seeds Activity ON for integrable CLIs (server now forces mechanism-less off with a never-persisted copy + predicate seed); the toggle first gated on plan branches which hid it for grok/hermes/opencode too (fixed via hasIntegrationMechanism row field).
Test saga: TestHookMapAgyReport failed 3x under sharded runs and never standalone; root cause was ambient PICODE_TUI_PID/RUN_ID inherited from the agent terminal running the suite (mine) engaging the observation gate while the harness strips TERM_ID, so the recorder silently dropped the report — fixed by pinning empty TUI vars in the test, following the native_session_test pattern. Not load flake, not product code.
Verified: mapper table, merge/install round trip, reporter e2e with stub hook server, prepare without wrapper, PUT on/off round trip with applied flag, desktop screenshot read (toggle on, no note), mobile note verified earlier for the no-mechanism shape. Seed bumped v1 to v2 so existing installs pick agy up; owner-switched-off stays off. Still missing: nothing in launch parity except muse activity (needs managed agents). Deploy NOT done (owner's call).
visual-review: PASS (var/screenshots/agy-launch-toggle.png read; card 5/5)
