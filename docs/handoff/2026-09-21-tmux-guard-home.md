# 2026-09-21 — feat/tmux-guard-home: tmux guard switch moved to Agent CLIs
Shipped: the tmux guard switch moved from Terminal defaults (its Safety section) to Agent CLIs, above the CLI catalog, on desktop and mobile (TmuxGuardRow.jsx in web/browser and web/mobile; .cli-guard styles). Terminal defaults lost the Safety section, its guard CSS (.termset-guard*) and the "tmux guard" menu subtitle; menu search for "guard" now surfaces Agent CLIs (userMenuModel). Copy states the row covers every PiCode terminal, not the CLI selected below, and applies to terminals opened from now on. Docs: docs-site/guide/agent-clis.md, docs/architecture/terminal-bridge.md, docs/handoff/open/terminal.md (desktop-only trap corrected), fragment docs/changelog.d/tmux-guard-home.md.
Verified: make ci-scoped PASS twice (fmt, vet, hooks, test-js, build, docs), including after the merge of main. node --test userMenuModel.test.js green. Visual review on a scratch instance (scripts/qa-scratch.sh): guard row on Agent CLIs (on, off, 390px narrow), overlayAudit ok:true, Terminal defaults starting at Appearance with no Safety section. Method blind spot: the failed-refresh state of the new row was not captured (the old page's error notice component is unchanged); toggle exercised via browser only, not through the Windows desktop shell.
visual-review: PASS (tmux-guard-clis.png, tmux-guard-off.png, tmux-guard-narrow.png, tmux-guard-termset.png; card 5/5)
Not done / debts: none known.
Merge: fast-forward ready (main at b73d85c7 can ff to 2bf93a8b).
