# 2026-09-23 — worktree-interlock

- **Owner decision:** approved letting the worktree-creating command past ADR-0078's busy-repository interlock ("aprovado, esse comando está liberado"). Recorded as ADR-0202 (accepted), with an Amended line on ADR-0078.
- **Done:** `gitcmd.WorktreeCreate` recognizes exactly `git worktree add -b <slug> <root>/.worktrees/<slug> <ref>` (quoted root without inner quotes, slug one segment, ref valid, nothing appended); `POST /api/terminals/{id}/run` skips `repoBusy` for it when the root is the terminal's own repository. Shell prompt, root and foreground checks stay. Docs: agent-manager (run door), cli-session-handoff (Fork), docs-site guide; the agent-fork open debt is paid.
- **Verified:** gitcmd table (composer round trip; chained, relative-in-quotes, other folder, flag-as-ref, trailing words refused); `TestTerminalRunRefusals` (busy + exact command passes the interlock, other root / appended command / no `-b` stay 409 busy); `TestTerminalRunCreatesWorktreeWhileBusy` with real tmux and git: an agent mid-turn, the worktree still appears.
- **Live (scratch, real Claude Code):** with the source agent mid-turn (state `working`), Fork agent… → New worktree created `.worktrees/calc-fork` on branch `calc-fork` and opened the fork with no Enter prompt. The dialog's "prepared" fallback stays for a terminal that cannot run the command.
