# 2026-09-24 — exit-insights-study

Study for option B of the exit feedback loop (`docs/benchmarks/2026-09-24-exit-insights.md`),
sized against the production catalog read-only: 17 exits in ~22 h, one
workspace, 8 answers all "resolved", no reasons, and `model` empty for every
terminal CLI's exit.

## Next up

- Owner decides the thresholds, the New agent line, and B0 (model at exit) before B1 (insights on read).
