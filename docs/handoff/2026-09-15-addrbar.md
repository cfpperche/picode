# 2026-09-15 — feat/addrbar: the frame's wasted line and the address pill

Owner review of the dev-server preview, two items with screenshots:
1. "The desktop app shows this page in its own window; here it runs in a
   frame." is gone. It only ever rendered in a plain browser (the native
   webview needs no explanation), and it spent a row of a 1080p window on
   every local page. The frame now starts directly under the toolbar; the
   non-local case still explains itself once a URL is typed.
2. The address pill's Open button: `.web-tab-toolbar button` gives the
   toolbar's buttons `height: 28px`, and the pill's own rule never overrode it,
   so the hover grey covered a 28px strip inside a 36px pill (owner screenshot
   `155646`). It now takes `height: 100%` — the pill's `overflow: hidden` and
   `border-radius: 999px` clip it to the rounded end, the shadcn ButtonGroup
   shape. The `↵` text glyph (high in its box on most fonts) became lucide's
   `CornerDownLeft` (`IconEnter`), so the box centres it.
Docs: `docs/architecture/devservers.md` corrected (it promised "one line
saying so"), changelog fragment. Verified in a scratch of this branch
(:8476, `var/demo/devserver-site` on 5174 so the merged main's demo keeps
5173): the frame has no note, the pill's hover fills and rounds, the return
glyph is centred — screenshots `var/screenshots/addrbar-{noteless,hover}.png`.
Not verified: the shell's own WebView2 path (no Tauri host here); the CSS is
shared, so the shell's pill changes with it.
visual-review: PASS (5/5)
