# 2026-09-21 — agy-sweep-docs: the sweep closed with `agy`, and the record now matches it
The owner's correction (`Antigravity` is `agy`) closed the last row: the catalog entry is `{"agy", "Antigravity", "agy", …}`, so the earlier "no binary on this machine" was my probe looking for the wrong name. `agy` 1.2.8 was installed all along; with its OAuth token in a scratch HOME, `agy -p … --dangerously-skip-permissions` ran, its stdio MCP child inherited the launch environment (probe: the `PICODE_*` set), and an `agy` agent declared **"agy probe"** — `principal: agyqa-99b5c8`, the project read shows `agents: ["AgyQA"]`.
With that, all **eight** catalog CLIs whose binary is on this machine have exercised the tool end to end (Claude Code, Codex, OpenCode, Grok, Omp, agy, Hermes, Muse); the delivery-flow debt line says so, and `docs/architecture/picode-mcp.md` now carries the per-CLI env table instead of the three-row hint: five CLIs inherit the launch environment, Codex needs the values written into its `-c` overrides, Hermes and Muse need them in their own server entry (their connectors, 13- and 8-variable allowlists).
Still open, unchanged: a connector at user/workspace scope has no per-terminal identity to write for Hermes and Muse; OpenCode's live call waits on a model the owner's plan covers; no CLI has been run against a live model with the owner watching.
