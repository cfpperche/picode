# 2026-09-11 — fullscreen-fixes: the rail from the strip, an icon-only exit

Shipped: `reveal { zone, pinned }` and `rail { open }` in `web/shared/domain/focusMode.js` (+6 decision-table rows, 38 pass) with their wiring in `lib/useFocusMode.js` — the mode now watches `#inspector[hidden]`, so `railWasOpen` is no longer only the snapshot taken at entry. In fullscreen the strip's Inspector toggle shows the rail as a pinned overlay: showing docks it when it was closed (the panel has to exist), hiding takes only the overlay down, and `Ctrl+.`, the context menu and the palette take the same path (`App.jsx`: `toggleInspector`, mode-aware `shown`, `railFits`). `FocusLeave` is icon-only (28×28, 16px glyph), label and chord in the hint. Docs: the focus-mode paragraph in `docs/architecture/routes.md`, `docs-site/guide/getting-started.md`, `docs/changelog.d/fullscreen-fixes.md`. No ADR: a UI refinement crossing no boundary (ADR-0105).
Verified: `make ci-scoped` and `make close` green (fmt, vet, hooks, test-js, build, docs); 659 JS tests. Browser QA on a scratch instance (`qa-scratch.sh fsfx`, 1600×900, terminal tab): rail closed → the toggle reveals it and the right edge strip starts existing from then on; rail docked → reveals, dock preference untouched (`open=1` in, `1` out); the panel survives the pointer leaving for 500 ms (pinned) and leaves on the toggle and on Escape; the left and top strips still work while it is pinned; a real click on the icon-only exit leaves the mode; a 900px window keeps the honest "Window too narrow for the inspector" title and does nothing; reload restores the mode; `__picodeOverlayAudit()` ok (rows 28px, tops aligned at 6), console clean but the expected "browser refused fullscreen" debug line for a synthetic chord.
visual-review: PASS (6 captures read: dark and light strip with both 28px controls, dark and light rail over a near-black terminal; no clipping, alignment holds; the exit glyph went 14→16px after the first read showed it a size smaller than its neighbour).
Not done / debts: touch has no hover, so the exit and the toggle show no label there (hint-only) — the first-run toast still names Esc; the rail's right strip stays pointer-transparent (embedded PDF preview), and Escape/browser-fullscreen behavior is unchanged. A *hover* reveal still behaves as before: moving from an open hover reveal to another edge closes it instead of arming the next (a pinned one arms the next).
Merge: fast-forward ready.
