# 2026-09-18 — annotate like the reference (feat/annot)

Built `docs/handoff/open/work-browser-tabs.md` §"Annotations — the target"
literally: strip with Send N, numbered pins, accumulating notes, anchored
card with Save, chips, one-package Send. Acceptance screenshot:
`var/screenshots/annot-mode.png` (strip + Send 2 + pins 1–3 + chips + card).

## What landed
- In-page script: multi-note state, card (icon/input/trash/mic/Cancel/Save),
  chips (text/…/×), state syncs; mic uses SpeechRecognition when present.
- Shell: `btab_annotate_clear` (+`last` for undo); `btab_annotate_mode`
  added to the build manifest (it was ACL-dead). Rust tests: 6/6.
- Chrome: strip replaces the URL bar; batch Send (one row per pin, ONE drop
  message). Fixed missing `liveUrlRef`. JS tests: 8/8 annotate, test-js green.
- Fallback preview (labeled) over a sample page — the screenshot surface.
- Found by QA: event read after release blanked the app on the 2nd pin.

## Next up
- Owner's live run on Windows: mode, strip, batch Send to a running agent.

## Debts
- Batch Send never ran against a real terminal (Windows-only shell path);
  unit-tested + reviewed, live run is the owner's.
- Remaining target slices: style inspector, settings row (step 5).
