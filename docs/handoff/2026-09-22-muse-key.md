# 2026-09-22 — feat/muse-key: the only ubuntu failure on a frozen main was a rename that half-landed

One commit. The owner stopped every other session so a matrix could measure a still `main` (af2d029e) — the first honest reading of the day, since earlier runs each measured a different commit.

**What the frozen run said.** Windows green. Zero timeouts anywhere. ubuntu: **one** failure. macOS: eight.

The ubuntu one was `TestConnectorListToleratesJSONC/muse_user`, and it reproduced locally, so it was never an environment problem. `8febb603` moved muse's MCP block from `mcp_servers` to `mcpServers` — the key muse actually reads — and updated `muse_test.go` and `server/mcp_test.go` but not `codec_test.go`, which kept seeding a document the driver no longer looks in.

Worth being clear about: **the test was correct and did its job.** `servers = []` is precisely what a reader aimed at the wrong key returns. There is nothing to harden — the fixture was simply behind the rename. (Hermes' YAML and Grok's TOML keep their own `mcp_servers`; those are vendor spellings, not leftovers.)

**macOS, on a frozen main, holds eight** — three devserver tests, `TestGitWatchTickWorktreeAliasUnderSecondSpelling`, two `TestNativeObservation*`, the same connectors row, and `TestStopIdleFencesConversationAndCommands`. That flake has now failed on macOS twice and passed on ubuntu once, so it is a flake rather than a platform fact.

Verified: `make close` green, scoped. visual-review: n/a. Nothing deployed.

## Next up

- macOS has never been measured green. With ubuntu and Windows clear, the seven macOS-only failures are the whole remaining gap and none has been diagnosed.
