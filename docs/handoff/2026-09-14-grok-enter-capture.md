# 2026-09-14 — grok-enter-capture: settle window for the withheld Grok Enter

Shipped: the post-paste composer check is now polled inside a bounded 1.2 s window (`peerAwaitComposer`) against the unchanged guards; Enter still goes only after one full-frame match. Structural refusals (pane, runtime, connection, fit) return immediately; expiry refuses uncertain exactly as the old single 150 ms sample did, never retried, drafts untouched. Refusals are classified without screen content (`paste not rendered yet` / `composer changed or is not ready` / `pointer does not fit` / `pane changed`), so a recurrence names its guard in the log. Plan: `docs/plans/grok-communication-input.md` (settle-window section + decision-table rows).
Verified: `make ci-scoped` green incl. new table tests (`TestPeerPasteSettleWindow`, refusal-classification rows). Native on scratch `localhost:8471`: `check_SSVJYDHRLMC4VKP4VAAO6INZ6U` **passed** — managed Pi → Grok 1.0.30, paste → settle → Enter once, native read, correlated `OK` reply, both ACKs; a second check with a live draft expired after 5 min with the draft byte-identical and zero pastes. Probe (`var/qa/grok-enter-capture/probe-result.json`): real Grok settles the full frame 24 ms idle / 30 ms @8 busy loops / 44 ms @16 — synthetic CPU load did **not** reproduce the matrix contention; the window covers the observed settle times ~27×.
visual-review: n/a (server behavior, no UI)
Not done / debts: the original matrix contention was not reproduced, so the window is validated by tests + classified logging rather than by a reproduced failure; pre-paste single-sample check left as-is (self-healing: refusals there stay pending and retry). Scratch sessions were not touched beyond my own.
Merge: fast-forward ready (`cd /home/goat/picode && git merge --ff-only feat/grok-enter-capture && make ci`).
