# 2026-09-21 — feat/pkgs-ux: the five guest-packages refinements, and the scope id a missing one cost Claude Code
Shipped (ADR-0167), the owner's five proposals for the guest Packages pane: installed rows group by origin in the vendor's own word (`sourceKind`) — Hermes `Installed by you 1` / `Ships with the CLI 2`, Claude Code `From a marketplace 2` / `Managed by claude.ai 1`; a single-origin list gets no header (`groupInstalledRows` returns nothing for one group), and specific kinds match before the marketplace fallback, because Claude names a synced plugin's marketplace `synced` too.
Filter highlight: `matchParts` splits a name/description around the needle and the matched run renders as `<mark class="pkg-mark">` in the accent colour, a whole-name match included.
Marketplace chips (catalog card and sources row) filter the catalog and clear on a second click (`aria-pressed=true`, 30 of 63 rows).
An empty installed list offers up to three real catalog entries with Install — the vendor's own data, read once for that — plus `See the whole catalog`, only where the CLI has a catalog and can install.
Both tabs pin one `position: sticky` block holding the filter row and the availability check (the marketplace catalog runs to 63 rows / `scrollHeight 6153`; the bar held `top: 41` at scroll 900).
Fixed in the same pass: Claude Code's installed plugins had disappeared. The pane leaves `scope` out of the query for the machine scope, and `clipkgs` passed that empty scope to the vendor parser, which compares it with a row's own word (`user`) — every row dropped, so a CLI with plugins showed an empty list. `clipkgs.List`, `Available` and `CheckUpdates` now resolve the scope once (`normalizedScope`) and use the resolved id for the argv, the row filter and the cache key; regression `TestTheDefaultScopeReadsTheMachineScope` (`internal/clipkgs/clipkgs_test.go`) fails on the old code (`rows = 0`) and passes now (2 rows, plus the catalog's update badge).
Files: `internal/clipkgs/clipkgs.go`, `internal/clipkgs/clipkgs_test.go`, `web/{browser,mobile}/src/components/GuestPackages.jsx`, `web/{browser,mobile}/src/styles/app.css`, `web/shared/domain/cliPackages.js` (+ `.test.js`), `docs/architecture/cli-packages.md`, `docs/changelog.d/pkgs-ux.md`.
Verified: `make ci-scoped` PASS (10 paths; go[5], test-js, build); the JS suite's 22 tests pass; a scratch instance with stub `claude`/`hermes` vendors (untracked fixtures under `var/uxfix/`) read at 1500px (two columns `465px 465px`, groups, sticky bar pinned), 860px and mobile 390px (one column, zero horizontal overflow), `window.__picodeOverlayAudit()` `{ok: true}` on every state, screenshots read.
visual-review: PASS
Close-summary: 3 commits (82522277 + two merges of `main`), 10 files changed, 696 insertions(+), 143 deletions(-); scope `FULL= GO=1 WEB=1 PACKAGES= DOCS= METADATA=1`; in flight 3 ahead, 0 behind, clean, gates stale.
Merge: fast-forward ready (base `1002c770`, head `7e7d9efa`).
