# 2026-09-21 — feat/pkgs-qa: the update check survives a short list, and a group count is not a pill
Shipped (ADR-0167, c276818d): adversarial review of the guest packages pane found two defects. Check for updates lived in the filter bar, which rendered only when `rows.length > 1`, so Claude Code (1 real plugin, `caps.update`) and an empty list had no check. It now renders whenever the CLI has the verb. Group counts reused `.pkg-count` (bordered toolbar chip; measured border 1px and a filled background; screenshots read as a stray button). `.gpkg-group-count` is plain text (border 0, transparent). The installed list is one `role=tabpanel`, not one per group.
Files: `web/{browser,mobile}/src/components/GuestPackages.jsx`, `web/{browser,mobile}/src/styles/app.css`, `docs/changelog.d/pkgs-qa.md`, `docs/architecture/cli-packages.md`.
Verified: scratch qa2 on :8473, symlinks to the real `~/.claude` `~/.hermes` `~/.grok` (read-only; no install/remove clicks). Claude shows Check again; Hermes groups "INSTALLED BY YOU 1" / "SHIPS WITH THE CLI 59" with plain counts; Grok empty still has the check and 3 catalog picks; `overlayAudit` ok. `make ci-scoped` PASS (6 paths). Screenshots `var/screenshots/adv-*.png` (gitignored). Blind spot: not run inside the Windows shell; close-summary recorded no green full run.
visual-review: PASS
Held, not failed: sticky bar at top 41 while Hermes (60) and Grok marketplace (338) scrolled, opaque; Enable stays opacity 1 while a disabled card's text is 0.62; highlight is accent text on transparent (computed); mobile 390 is one 330px column with 0 overflow; Muse 502 is one line + Try again. Source-management chips scroll away (`sourcesTop` -796); card marketplace chips remain.
Not done / debts: none new (no topic-file edit).
Close-summary: 1 commit, 6 files, +27/−12; scope `FULL= GO= WEB=1 PACKAGES= DOCS= METADATA=1`; in flight 1 ahead, 2 behind, clean, no green run recorded.
Merge: not fast-forward (base `75f60adb`, head `c276818d`; main had moved). Merge main in this worktree, rerun `make close`, then fast-forward from the root.
