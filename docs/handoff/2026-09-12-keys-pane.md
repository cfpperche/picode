# 2026-09-12 — feat/keys-pane: the keyboard map leaves the sub-tab row

Owner's review, same day as the pane shipped: sub-tabs inside sub-tabs read as
clutter. The map is the **Keyboard** pane now — `#/clis/pi/keyboard`, between
Settings and Packages — and Settings has no sub-tab row. The name was chosen
against *Keys* (reads as credentials one pane from Providers), *Keybindings*
(jargon), *Shortcuts* (PiCode's own chords, under Preferences).

Root cause of the report that started it: the click *did* work, then the pane's
"carry the selected agent" effect rewrote the hash through `cliSetupHref`,
which rebuilt the settings href and dropped `tab=keys` — visible only when the
route had no `agentId` and the sidebar held a workspace or terminal (a fresh
harness session never had that context). Fixed here: the pane is a route (no
param to lose) and `cliSetupHref` carries `layer`; `cliPaneSetupContext` learned
`layer` too.

Verified: `web/shared` domain tests (24 in cliSettings/cliLaunch, new rows for
the pane, the context it carries, the legacy redirect and the malformed path);
`scripts/qa-cli-settings.mjs` **35 rows** (desktop + mobile: the Keyboard pane
opens from its own tab, a captured chord is saved to the machine map, the old
`?tab=keys` link lands there, and a Keyboard click survives the pane's route
rewrite with a terminal in the sidebar) and `qa-cli-settings-recovery.mjs` all
green — two rows' evidence changed honestly with the split: the map is now
asserted *usable* while a settings read fails (its own file, its own endpoint),
and the failing read is triggered on the Settings pane instead of the map.
Screenshots read: desktop Settings (no sub-tab row), desktop Keyboard, mobile
Keyboard, 560 px pane bar. `__picodeOverlayAudit()` ok in every capture.

visual-review: PASS

Not done / debts: the pane bar scrolls horizontally at 560 px with 8 tabs — the
active tab is scrolled into view, the rest are one swipe away; making the bar
tighter or grouping the setup panes is a bigger call, owner's.

Merge: fast-forward ready.
