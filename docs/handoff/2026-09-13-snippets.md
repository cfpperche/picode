# 2026-09-13 — snippets

- ADR-0130 + `internal/snips` (Parse skips `{{{{`, Expand unescapes, Slug
  hyphenates), migration 047 `snips`, `/api/snips` CRUD + `/picker` +
  `/expand` + `/run`, `snip.*` summary events, reducer `applySnips`.
- Desktop `#/snippets` studio; composer `/snip:` (Insert/Send —
  `fireSend(spliced)` keeps images); palette **Send snippet**;
  `/run` agent target SendTurns with live cwd/workspace/agent.
- Doors: prompt→CLI via shared `pasteToTerminal` (0089 untouched);
  **B6b**: bare-shell foreground 409 `kind` only when the wrapper lease
  is gone — a live launch's pane leader is `sh` by construction, so the
  gate checks `TermRuntimes`. Shell kind = snippet-run door (confirm +
  `preview:true` shows the exact command; live lease → 409 `cli`;
  foreground → 409). Menu rows follow the pane: **Send to terminal…** on
  CLI panes, **Run command…** on shells.
- Mobile: Pins-shaped hashes (`#/more/snippets`, read, edit), terminal
  actions rows, composer picker fetched outside the stopped gate.
- QA: ci-scoped PASS (66 paths); verified live on scratch — prompt
  snippet pasted into a real pi TUI ("Look at 77/123." in-pane), shell
  command ran in bash after confirm; `__picodeOverlayAudit()` ok on all
  sheets. Pixel verdicts UNVERIFIED (this harness cannot read images) —
  geometry + DOM only.
- Adversarial round same day: archive/star refetch, preview no longer
  logs `snip.ran` (and is prompt-kind 400), `{{branch}}` resolves on
  `/run`; enum-400 + branch + preview tests added. Details in
  `docs/handoff/open/snippets.md`.
- Simplifications vs design: palette shell rows skipped (term-menu is
  the shell door); enums editable only server-side until an editor
  needs them. Debt: none blocking.

## Next up
- Post-merge smoke on the deployed instance is the owner's (`make deploy`).

## Debts
- `docs/handoff/open/snippets.md` — visual pixel PASS owed from a
  harness that can read screenshots.
