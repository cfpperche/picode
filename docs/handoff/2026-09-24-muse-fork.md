# 2026-09-24 — muse-fork

- **Asked by the owner:** item 1 of the agent-fork list — Muse Code fork.
- **Measured (Muse Code 1.3.0):** `muse serve` speaks MSP (JSON-RPC, one object per line); `initialize` → `initialized` notification → `session/fork` copies a session in < 1 s, also while its TUI is open, and also a running session not yet indexed. The copy opens with `muse resume <id>` in any folder (the workspace follows the cwd); `muse resume` takes no prompt; `--resume` is refused ("unexpected argument"). Muse indexes a session (and `session/list` shows it) only when its TUI exits, and creates the session when the TUI starts.
- **Done:** `clisession.SessionForker` (Muse: MSP fork, `Fork{TaskAfterLaunch}`), `LiveSessionFinder` (session dirs written after the current launch start, minus other pins and fork copies, confirmed by `session/read`); server `forkAgent` runs `muse serve` through the CLI's configured command (`cliCommand`), pins the source if needed, delivers the task through the prompt door with retries (`deliverForkTask`, 3 min, Inbox note on failure). Menu offers Fork on a running terminal with no runtime `cli`. Muse `ResumeArgs` → `resume <id>` (fixes Resume last session / Continue in… to Muse on 1.3.0).
- **Verified:** unit tests with fake `muse serve` (fork, refusal, wrong source, live lookup with exclusions); server test (fork request, `resume` launch, pin, lineage, task reaches the TUI); scratch instance with real Muse 1.3.0: forks from the UI and the API received the task and answered the source's word (PINEAPPLE, MANGO); two forks in the same folder both copied the source. First live pass pinned the wrong session (a stale one from a removed terminal); fixed by the strict launch-start cutoff and exclusions.
- **Not covered by a server test:** `pinLiveSession` end to end (covered live and by the clisession unit test).
