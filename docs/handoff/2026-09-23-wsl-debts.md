# 2026-09-23 — feat/wsl-debts: pay the three Management debts in windows-wsl.md

Shipped (13fca291f): `.wslconfig` edits write one target section and leave copies in other sections untouched; disk-compact disables and re-enables the PiCodeDistro task; tool-owned caches are measured where the tool says (live: pnpm store at `~/.local/share/pnpm/store/v11` is 1.3 GB, was 8 KB at `~/.cache/pnpm`). All three flipped to `[x]` in `docs/handoff/open/windows-wsl.md`.
Adversarial review: the first fix 1 deduplicated a key present in both `[wsl2]` and `[experimental]` by keeping the first copy; since the page resends every key on save, that could silently flip which value WSL honours. Also from it: tool queries run from home with `GOTOOLCHAIN=local` and no corepack download; answers outside home, on `/mnt`, equal to home or nesting another cache are refused; clean locates once so before/after compare the same folders; tool lookup is memoized per process.
Verified: `make desktop-test` (49), Go tests for the `LocateConsumers` decision tables, `make ci-scoped` green. Not run inside the Windows shell; live only after `make deploy` (distro picode) and `make desktop-restart` (shell, tool).
visual-review: n/a
Merge: `make close` had not run when this note was written.

## Debts

- A killed disk-compact/move/backup leaves the PiCodeDistro task disabled until the shell's next ensure re-registers it (`Register-ScheduledTask -Force` re-creates it enabled).
- Which copy WSL honours for a key duplicated across `.wslconfig` sections is unverified against WSL's source.
