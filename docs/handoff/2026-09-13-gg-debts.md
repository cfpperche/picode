# 2026-09-13 — feat/gg-debts: the watcher's crash, and the picker's two unverified rows

Shipped: `gitWatchTick` (`internal/server/git_watch.go`) skips a path that left the watch set — removing a workspace used to panic the daemon; `TestGitWatchTickSurvivesARemovedWorkspace` fails on the old code and passes now. `lib/gitWorkspacePicker.js` gained `pickWorkspace` (the pick with its two failure rows), `ownerIdOf` (the state key: kind + id), `ownerRoute`/`graphUrl`/`headUrl` (the two requests, route + plain id), and the graph surface builds every request from them.
Verified: `make ci-scoped` PASS; 309 browser lib tests (5 new) + the Go regression test; scratch `gg1`: the removal repro answers health `200` where it panicked, and the browser flow ran live — reset-soft through the graph menu, the pending banner settling into the Undo row, then a workspace switch that left neither (var/screenshots/ggd-2…ggd-6.png).
visual-review: PASS (var/screenshots/ggd-1…ggd-6.png read; overlay audit ok; card 5/5)
Not done / debts: the state key slipped into the request URLs twice while this branch was written — a 404 graph load and a pending watch that never saw the token. Both are pure, tested helpers now; neither shipped (the picker branch used the plain id).
Merge: fast-forward ready
