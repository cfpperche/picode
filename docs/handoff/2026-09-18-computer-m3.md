# 2026-09-18 — computer-m3: computer use shows itself

ADR-0148 M3 (`docs/plans/computer-use.md`). Three surfaces and two pages:

- Chat: each computer step's capture lands as the step's "Last capture"
  (the `agent_browser` path, `details.preview` with source and time), and
  the work list labels the step by its action ("Computer: left_click
  [412,88]"; `toolArgs.js`, `turns.js`).
- Dashboard: `desktop` on `GET /api/sessions/stats` — steps the shell ran,
  their time, calls refused or failed — from `Store.EventsOfTypeBetween`
  over `computer.step`; a Desktop panel beside Agent time.
- Audit: a refusal for an unknown agent id is now recorded (found by test).
- Docs: `docs-site/guide/computer-tool.md` (what the switch means, the 23
  actions, who gets a switch), `docs/architecture/computer-tool.md` (who
  decides what, the line, the actuator, the limits), index and menu rows.

Verified: Go tests, JS suites, `make web`, Vale. Not verified live: the
panel and the capture card — the owner's first real run covers both.

## Next up

- Owner: after the first real run, open the dashboard's Desktop panel and
  the step's capture in the chat; read the public guide page.
- M4 (`feat/computer-m4`): the eval suite (12–20 Windows tasks, pass^3,
  steps, time, cost), UIA refs + `stale_ref`, Graphics Capture fallback.
