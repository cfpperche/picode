# 2026-09-21 — hm-identity: Hermes and Muse, validated the way the owner asked
The owner's shape: build nothing; enable the tool at **workspace** scope, fall back to the **global** config where a CLI has no workspace layer, confirm what happens, discard the junk. Done on a scratch with throwaway agents: both drivers refuse the workspace scope in their own words ("Hermes keeps one config file …; there is no per-workspace file" / "Muse keeps one settings file …"), the global card was accepted, and both CLIs then answered `no identity` — the same sentence from each — and neither declared anything.
Why no fix: those two hand their MCP servers only an allowlist of the environment (thirteen and eight names, none of them PiCode's), and neither exposes a per-launch config override (checked 2026-09-21: Muse offers only `config validate --plane`, an administrative document; Hermes no config-path flag). Their MCP config is a single file with no workspace layer, so a launch-time write would clobber another terminal's entry. Claude Code, Codex, OpenCode, Grok, Omp and Antigravity need none of this.
Recorded: the debt line carries the measurement and the decision (a documented per-terminal manual entry until either CLI grows an override or a workspace layer), the architecture page states why the launch cannot write it for them, and the public guide's Connectors section gained the steps a person performs — Environment variables `PICODE_TERM_ID`, plus `PICODE_AGENT_ID` when the terminal belongs to an agent and `PICODE_DATA` outside the default data directory, one entry per terminal named after it.
Nothing was built; no code changed. Scratch, throwaway repository, copied credentials and temporary files were discarded.
