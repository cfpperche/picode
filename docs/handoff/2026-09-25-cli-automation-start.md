# 2026-09-25 — cli-automation-start: automation `start` runs on Claude Code, Codex, Grok and Hermes

Scope found: Fatia F (message automations and the pane ask through the prompt door, with receipts) had landed 2026-09-20 (`284eaac7e`); `open/managed-principals.md` was stale and is marked done. What remained: `start` runs were Pi-only. Owner's three calls: never pass an auto-approve flag (a needs-you files an Inbox question and the run waits); v1 CLIs are the four with composer readers and hooks; the terminal closes at the end.
Shipped: ADR-0217 (proposed). `automations.cli` (migration 074, default `pi`; `store.UnattendedCLIs`, JS `START_CLIS` pinned by `TestStartCLIsMatchTheEditor`). Runner `internal/server/automations_cli.go`: `ensureCLIAgent` (created on the first run, reused after), `freshCLITerminal` (kill + `ensureShell`), `driveCLIRun` — door retried every 2 s for 90 s while the refusal can clear; a verified receipt or a hook state within 15 s of the paste (`hookSawPrompt`); `turnWatch` (idle ends the turn, each new needs-you files one Inbox question); cost from the session file every 30 s against the cap; 2 h timeout; close through `killTerminalPane` (the ADR-0085 escalation, extracted from the stop handler). A start whose terminal is open skips as `agent in terminal`. Pi path unchanged; `ensureAgent` reuses only a Pi agent.
Composer readers re-measured live (`peer_attention.go`, fixtures in `peer_composer_state_test.go`): Claude Code 2.1.282 renders the empty-field suggestion word by word (cut at the edge without a reset) and adds an `◐ medium · /effort` row; Codex 0.157.0 adds a `? for shortcuts` row. Before the fix every unattended Claude/Codex run failed as unrecognized/diverged, and the attended door fell back to unconfirmed/blind paste.
UI (desktop + phone): CLI select in Start mode; Pi's provider/model/thinking only for Pi; per-CLI hint; detail fact "CLI: X, with its own settings". Run reasons wrap under the pill (were clipped, and overflowed the phone table); failure reasons are plain language naming the CLI and the next step.
Verified: store/server/JS tests (decideFire rows, doorRetryable, turnWatch, hookSawPrompt, CLI validation, parity). Live on a scratch with copied logins: Claude Code 3/3 done (~$0.15 each), Codex 2/2 (~$0.12); Grok without a login failed with the new wording (the refusal path). Copied credentials were deleted from the scratch HOME after QA.
Blind spots: Grok and Hermes never ran a turn live (no login on the scratch); needs-you → Inbox is unit-tested only; the final message is not read back (the Inbox result points at the session); Claude's post-turn "Teach auto mode about your environment?" menu, if on screen at start, fails the run as unrecognized.
visual-review: PASS after two FAIL rounds (captures showed the list, not the detail; clipped reason pill and phone overflow; jargon reasons).
Not deployed (owner's call). Merge: through `make land` after merging `main` and rerunning `make close` (ADR-0124).
