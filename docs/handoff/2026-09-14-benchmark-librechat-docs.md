# 2026-09-14 — benchmark-librechat-docs: LibreChat docs as the public-docs bar

Shipped: `docs/benchmarks/2026-09-14-librechat-docs.md` (Take/Refuse,
audience IA, feature-page rhythm, first-run ≠ from-source). Indexed in
`docs/benchmarks/README.md`, bars 6–7 in `docs/benchmarks.md`,
`docs/guidelines.md`. Open topic `docs/handoff/open/docs-site.md`.
No `docs-site/` rewrite; VitePress stays. No ADR, no changelog
(study-only, precedent ssh-terminal-study).

Verified: receipts from live librechat.ai/docs and
`LibreChat-AI/librechat.ai` `content/docs/*.json` / MDX (2026-09-14).
`make ci-scoped` in the worktree.

visual-review: n/a (no UI).

Not done / debts: owner confirms Take/Refuse, then the next session
edits `docs-site/` (sidebar, getting-started, feature rhythm, config
overview, MCP cookbook).

Merge: fast-forward ready.

## Next up

- Owner confirms Take/Refuse; next session rewrites `docs-site/` against the LibreChat bars (`docs/handoff/open/docs-site.md`).
