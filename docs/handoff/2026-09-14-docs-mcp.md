# 2026-09-14 — docs-mcp: MCP cookbook

Shipped: `/guide/mcp` is the index. `/guide/mcp-gmail` and
`/guide/mcp-deepwiki` are the shipped connectors. Integrations keeps
webhooks. Closed `docs/handoff/open/docs-site.md` (LibreChat docs bar
done).

Verified: `make docs` + Vale. agent-browser :4179 — desktop light (MCP
index with nested Gmail/DeepWiki), dark (Gmail), phone 390×844 (lead +
drawer). Screenshots `var/screenshots/docs-mcp-*.png` (not committed).

visual-review: n/a (docs site).

Merge: fast-forward ready.
