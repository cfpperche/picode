# 2026-09-14 — inbox-address-recovery: legacy terminal questions get an address at reply time

Shipped: `DeliverTerminalReply` resolves an address for sessionless terminal-sourced items from the terminal's pinned last session (`TerminalLaunch.LastSession`), delivered only when the pin names the same file the receiver is showing — two independent sources, one answer. Disagreeing pins refuse naming the mismatch; unsafe and vanished pins flow through the existing safe-path/file guards; `system`-sourced items (pi outside the launcher) keep the honest refusal. Plan + decision table: `docs/plans/inbox-terminal-address.md`.
Verified: `make ci-scoped` green; new `TestInboxTerminalReplyResolvesPinnedSession` (delivery through the real reply-file + tui-ack machinery) and `TestInboxTerminalReplyPinnedSessionDisagreement` (mismatch refuses, vanished pin hits the existing guard); all prior `TestInboxTerminal*` rows unchanged and passing. Feed half of the debt retired with evidence — every ephemeral-rendering surface refetches on `feed.open`/`feed.reset` (messages view, reminders, checklists, mobile poll); durable events replay from the cursor; webhooks at-least-once is the delivery contract. `docs/architecture/routes.md` updated.
visual-review: n/a (server behavior; no JSX/CSS)
Not done / debts: no native run — 0.1.x-style items cannot be filed anymore with pi-inbox 0.2.0 installed, so the tests' real receiver machinery is the evidence; `open/communication.md` debt line rewritten (system-sourced refusal + park gap accepted; onboarding and credential-slot debts untouched).
Merge: fast-forward ready.
