# 2026-09-20 — tmux37: the host's tmux is 3.7c, and reports name the server's version

Implemented: ADR-0164 — the host's tmux is now 3.7c (built from source into `/usr/local/bin`; apt's 3.6a in `/usr/bin` untouched), and the reported version comes from the running server (`#{version}`) instead of `tmux -V`: `Manager.ServerVersion`/`VersionInUse`, `/api/system`, the tmux app's server view, and the drain now carries the answering (legacy) server's version and keyboard mode.
Measured, before and after: `PROTOCOL_VERSION` is 8 in 3.6, 3.7c and 3.8-rc, so the client/server skew is safe and the binary swap restarts nothing; DA1 sixel parity (`?1;2;4c`) holds once the build is `--enable-sixel`; the five terminal option defaults (`mouse`, `status`, `allow-passthrough`, `extended-keys`, `extended-keys-format`) are identical; both answer a pane's Kitty query with DA1 alone, so the forced `extended-keys-format xterm` stays correct. PiCode never calls `new-pane`/`split-window`/`new-window`, so 3.7's floating `new-pane` cannot change its splits.
Verified: `make ci-scoped` PASS on 027eda39 (fmt, vet, hooks, 8 Go packages, `internal/server` in 4 shards, longest 83 s); `make close` reused that green tree. Tests ran through `internal/tmuxtest` (private `TMUX_TMPDIR`, `TMUX` scrubbed), and the owner's server kept pid 15711 and its server version 3.6 throughout; the three sessions that appeared during the run all carry `PICODE_TERM_ID` markers (daemon terminals, owner working in parallel), so none is a test leak.
Limits: the running server is still 3.6, so floating panes need a server restart and ADR-0025's 3.6-era numbers stay true until then — the re-measure list is in `docs/handoff/open/terminal.md`, with the tmuxtest orphan-namespace finding (14 dirs, 7 live servers, one spinning pane since 19/09) and the build-parity notes (no systemd/cgroups; `/usr/local` shadows apt).
Deployment: not performed; deployment remains the owner's action.
