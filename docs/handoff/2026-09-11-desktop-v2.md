# 2026-09-11 — feat/desktop-v2: the Tauri shell skeleton (Phase 1)

Shipped (fast-forward ready, not deployed): `desktop-shell/` — crate
`picode-shell`, Tauri 2 + WebView2 (ADR-0120). Discovers the daemon address
through `wsl.exe` (`<data>/server.json`, first distro that answers, UTF-16LE
decoded like the Go half), opens the window on it, offline fallback page
(`ui/offline.html`), tray (Open/Quit, left-click opens), single-instance.
Build: `make desktop-shell` → `cargo xwin build` cross to
`x86_64-pc-windows-msvc`; default CI gates do not require Rust. Coexists with
the Go tray (no keepalive in the shell yet — Phase 2 migrates it).

Verified live: exe ran on Windows (PID 31688 at test time), window opened;
unit tests in Go unaffected; `make ci-scoped` PASS (full matrix).
`rustup target add x86_64-pc-windows-msvc` + `cargo install cargo-xwin`
bootstrap done in WSL; `llvm-rc`/`clang-cl` extracted without root into
`~/.local/picode-llvm` with wrappers in `~/.local/bin` (tauri-winres and
cc-rs need them under cargo-xwin).

visual-review: n/a for the Go repo — the shell window is the deliverable and
was opened on the owner's desktop for their eyes; screenshots of it are
theirs to share.

Debts: no WebView2-specific tests yet; the five Phase-1 spikes (pairing,
mkcert, xterm/WebGL, SSE, notification) are open; Go tray still owns
keepalive/disk actions.

Merge: fast-forward ready.
