# 2026-09-10 — main: browser surface phases 1–3 landed (and roles.json drop)

Shipped: browser surface end to end — watch-only live view (ADR-0114),
consent-gated control (ADR-0115), and Design mode pick-element; subsystem doc
`docs/architecture/browser-surface.md`. Deployed to production
(https://localhost:8445) after phases 1–2; phase 3 merged after deploy.
Also: `.pi/roles.json` dropped from the tree (`/roles clear`; workspace layer
of pi-roles goes dormant, agent overlays unaffected).

Verified: `make ci` green on main after each ff merge; every phase closed
through its own branch with visual QA (scratch instances, screenshots read,
overlay audits ok) — notes in `docs/handoff/2026-09-10-browser-*.md`.

visual-review: PASS (per-phase notes carry the cards)
Not done / debts: design-mode attachment is full-frame crop (element-bounds
crop needs DOM access); mobile stream view; remote scroll (engine wheel);
multi-viewer pacing policy; free-standing browser tab (omnibox per workspace).
Merge: this note closes the session.
