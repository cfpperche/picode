# 2026-09-13 — feat/tree-workspace-picker: the tree's folder line picks its workspace

Shipped: the Files toolbar's first item is the workspace picker the graph wears (`FileTreeSurface.jsx` header + `App.switchTreeWorkspace`, `file tree` listed every folder, not only repositories). The shared rule moved under a name that fits both surfaces: `web/shared/domain/workspacePicker.js` (`pickerOptions` gains `reposOnly`/`hint`, plus `hintPath` for a folder hint that elides from the head) and `web/browser/src/lib/workspacePicker.js` (`pickTarget`, `openRepoKeys`/`openTreeKeys`). ADR-0030 amendment; ADR-0022's reference to the old module name corrected.
Verified: `make ci-scoped` PASS; 307 browser + 654 shared + 356 mobile lib tests (the picker's own tests now cover both filters and the tab-id readers); scratch `twp` (a repo, a plain folder outside any repo, and a worktree) driven by agent-browser: the picker lists all three with the folder hint and a check, a pick onto the plain folder re-keys the tab to `d:/tmp/twp-plain` and the tree shows it with no Changes tab (no repository is a state), switching back restores "clean" and Files|Changes, and one workspace left keeps the plain folder line.
visual-review: PASS (var/screenshots/twp-{1,2,3,4,5}.png read; overlay audit ok; card 5/5)
Not done / debts: two defects were caught in the captures and fixed before commit — the graph's aria-label leaked onto the tree's trigger, and the row labels elided to "de…" because the hint competed for width (`flex: 1 1 0` on the hint, name unshrinkable, popover 340px, `hintPath` keeps the path's tail). Mobile Files still has no picker (same mould as the phone's Git sheet).
Merge: fast-forward ready
