# 2026-09-07 — ssh-docs: public guide "Agent terminals over SSH"

Shipped: `www/guide/ssh-terminals.md` (find `picode-*` tmux sessions, plain
and `-r` read-only attach, Tailscale SSH on tailnet boxes, the never-kill-by-
pattern rule from the 2026-09-06 incident, honest browser-only feature
table); sidebar entry under "Run it somewhere"; see-also at the end of
`remote-server.md`; CHANGELOG Added entry; `acls` in the vale vocabulary.

Verified: fact-checked against `internal/tmux/tmux.go` (default socket, no
`-L`; `picode-<agentId>` / `picode-sh-<terminalId>` naming); `make ci-scoped`
PASS including vitepress build (dead-link gate) and vale 0 errors.

visual-review: n/a (docs site only, no app UI).

Not done / debts: the optional terminal-card "copy attach command" affordance
from the SSH study stays deferred (owner call, UI decision); page deploy
ships with the next `make deploy-batch`, not this merge.

Merge: fast-forward ready.
