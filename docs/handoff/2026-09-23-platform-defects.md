# 2026-09-23 — feat/platform-defects: what the manual run found behind two dark legs

The dispatch that verified the macOS fixes came back with three red jobs, none of them caused by those fixes (they no longer appear anywhere in the failure list — their verification, by absence, is exactly the same shape as before).
- **`internal/server` had not compiled for Windows since the GUI sign-in paths landed.** `cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}` and `syscall.Kill` in `agy_login.go` and `opencode_login.go` — Unix-only API. The Windows leg compiles every package and test (ADR-0020), so a tag would have failed it too; it only runs on tags and manual runs, which is why a full-matrix run had not seen it. Both files now call `setLoginPgroup`/`killLoginTree`/`killLoginProcess` from `login_proc_unix.go` and `login_proc_windows.go` — the per-platform shape `agent_stop_*` and `process_zombie_*` already use. `go vet` is clean for linux, darwin and windows.
- **A fourth instance of the `/private/var` aliasing, in the dashboard's workspace attribution.** `TestWorkspaceForCwd/subdirectory` uses `parent/src`, which does not exist: `canonDir` could not resolve it and returned the path as typed, while the workspace roots resolved — so on macOS the two never matched. `canonDir` now resolves the deepest part that exists and re-appends the rest. Reproduced on Linux first (`TMPDIR=/tmp/alias`), where the test failed with the runner's exact message and now passes.
- **A llama flake**: `TestConnectionTimeoutAndTransport` failed once on ubuntu with the test's own listener refusing the connection; ten runs in a row pass, so it is a fixture timing race, recorded in `docs/handoff/open/llama.md`.
Verified: `make ci-scoped` PASS (fmt,vet,hooks,go[4]; 5 paths); `go vet` for three GOOS values; the workspace test 5× under the symlinked `TMPDIR`.
The last word is the dispatch that follows this push: Windows and macOS both run there, and both are what these fixes are about.
