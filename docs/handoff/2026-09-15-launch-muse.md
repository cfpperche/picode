# 2026-09-15 — launch-muse: editable Muse Code launch (Fatia 3a)

Fatia 3a of docs/plans/launch-muse-agy.md: editable Muse Code launch (owner approved 2026-09-15). SurfaceTerminal dropped for muse only; Antigravity stays terminal-only.
Spike verdict drove the shape: Muse Code 1.3.0 has NO hook surface — zero hook strings in the binary, plugins subcommand answers not available in this build, no user plugin path, settings.json holds only model/reasoning/permissions, enterprise planes absent — so the flip shipped WITHOUT activity: plan branch with summary only, terminals stay Open.
Toggle honesty: hasIntegrationMechanism on the catalog row (branches/files/env, not summary); web shows a one-line note instead of the switch; PUT defaults/overrides refuse integration:true with 400; prepare backstop fails clear for crafted profiles; startup seed skips hookless CLIs via a copied-never-persisted map. First version gated the toggle on plan branches and hid it for grok/hermes/opencode too (their mechanism is env/files) — caught on the scratch before shipping, fixed via the row field.
Caught by gates: the store seeds Activity ON for integrable CLIs, which would have broken every default muse launch — fixed at the source; two stale tests updated to the new contract (catalog surface/integrationCapable, PUT 200).
Also closed the known debt: tmux-inside launch failure now tested (unbindable socket path, no session created, attempt persisted with the fixed diagnostic).
Verified on scratch: Launch tab editor + note, row menu gains Launch settings (screenshot), PUT 400/200, mobile note, terminal creation 201 no launchError.
Still missing: activity (Fatia 5), writers (Fatia 4), agy flip (3b). Note for Fatia 5: the seed flag already reads 1 on upgraded instances, so a future muse mechanism needs a seed revisit.
visual-review: PASS (var/screenshots/{muse-launch,muse-row-menu}.png read; card 5/5)
