# 2026-09-21 — feat/ci-green: the last two blockers, and neither was a test that tested the wrong thing

One commit (4121fced), from the matrix run that verified `feat/ci-env-tests` and `feat/tmuxtest-revert`.

**The timeout moved rather than went away.** Sharding `internal/server` left `internal/store` running in one process under `-race`, where `TestEveryMutationAppendsAnEvent` alone takes 2m30s and the package passes `go test`'s 10-minute default the same way its neighbour did. `scripts/go-test.sh` now takes a *list* of heavy packages. Measured with `-race`: longest shard 415s for store, 484s for server, both inside the limit — unsharded, store needs roughly four times what it is given.

**Windows `go vet` died on `undefined: syscall.Kill`** in `cmd/picode-docs-fixture` (from `12c78d40`, another session). The shutdown test watches the pane's own process, which is a Unix call; it moves behind `//go:build !windows` so `TestPortSuffix` still compiles there. That job exists to compile every package and test on Windows, so excluding the one test that cannot beats excluding the file. `GOOS=windows go vet ./...` is clean over the whole tree.

What the verifying matrix established, worth carrying forward: every test fixed in `feat/ci-env-tests` is green on real runners, and `TestStopIdleFencesConversationAndCommands` passed too. It was a flake — leaving it alone instead of wrapping it in a retry is what let anyone find that out.

**Another session is working the same matrix** (`61ae0589 ci: stabilize cross-platform matrix`, touching `internal/tmux/catalog_test.go`). Two of the ubuntu failures in that run are in that file. Whoever picks this up next should look there before assuming the ground is still.

Verified: `make close` green, full scope; `GOOS=windows go vet ./...` clean. visual-review: n/a — no UI diff. Nothing deployed.

## Next up

- A push only runs ubuntu; the macOS and Windows jobs need `gh workflow run CI --ref main`. The verdict on this branch has not been taken yet.
