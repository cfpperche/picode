# 2026-09-21 — muse-mcpservers: the Connectors pane wrote a key Muse drops
Found by the vendor-session sweep that closed the delivery-flow debt's open half (see `feat/codex-tool-identity` for the Codex identity defect it also found). `internal/connectors/muse.go` wrote the MCP block as `mcp_servers`; the installed CLI reads **`mcpServers`** — measured three ways: `muse mcp --help` says "a streamable-HTTP entry under mcpServers in settings.json"; a file with only the snake_case key spawns **no** MCP server (no probe dump at all); a file with both keys makes Muse drop the whole member. The driver's doc comment and `docs/architecture/mcp.md` both claimed the block key had been verified against the vendor command in the 2026-09-18 phase — that claim is corrected in place, with today's measurement recorded.
Fixed: the key is `mcpServers` in the driver and its fixtures (`internal/connectors/muse.go`, `muse_test.go`), and the architecture paragraph now carries the measurement instead of the assertion. Muse keeps `schema_version` behaviour unchanged.
Verified: driver tests pass; on a scratch built from this branch the Connectors API (`POST /api/mcp`) wrote `~/.config/muse/settings.json` with `mcpServers`, and Muse spawned the servers from that file (probe dump present).
Not fixed here (recorded in `docs/handoff/open/delivery-flow.md`): Hermes and Muse hand their MCP servers only an allowlist of the environment, so a PiCode tool server connected through the Connectors pane still answers `no identity` — a per-server literal `env` works for both, but that value is per-terminal and a user/workspace connector has none to write.
