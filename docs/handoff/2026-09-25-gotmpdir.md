# 2026-09-25 — gotmpdir

`/tmp` (16 GB tmpfs) filled: `make ci` on main failed three times at the link
step ("no space left on device") during the wrapper-loop land. With the
owner's OK: removed session scratchpads idle ≥ 3 h with no open handle
(4.7 GB) and 1,041 stale `Test*`/`go-build*` dirs (1.85 GB); /tmp went from
95% to 34%. `scripts/go-test.sh` now defaults `GOTMPDIR` to
`~/.cache/picode-gotmp`, so test links no longer compete for that memory.
Rule noted in `docs/agents/processes-and-tmux.md`.

With TMPDIR unset, go test roots `t.TempDir` under GOTMPDIR too (measured),
so test scratch left the tmpfs as well; tmux sockets stay under /tmp.

The land also found main red, unrelated to this branch: two fork tests
assumed Omp has no screen reader, which ADR-0217 (`c4e61d7b2`, another
session) changed after the fork change (`caf7641b7`). Tests updated: Omp's
fork task now goes through the prompt door; the launch-argument path and its
length limit are tested with Omp's reader switched off.
