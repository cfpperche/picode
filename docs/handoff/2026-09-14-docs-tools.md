# 2026-09-14 — docs-tools: Where / Not this on Tools docs

Shipped: browser tools, Chrome extension, Docker, Integrations, tmux,
CLI activity reporting, keyboard. Feature-page rhythm on Use is done.
Left: config overview + MCP cookbook.

Verified: `make docs` + Vale. agent-browser :4177 — desktop light
(Docker), dark (`html.dark`), phone 390×844 (tmux lead + Tools in the
drawer). Screenshots `var/screenshots/docs-tools-*.png` (not committed).

visual-review: n/a (docs site).

Merge: fast-forward ready.
