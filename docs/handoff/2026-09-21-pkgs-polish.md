# 2026-09-21 — feat/pkgs-polish: the guest-packages pane, measured against each vendor
Shipped (ADR-0167): a refusal carries the command it refused — `clipkgs.Command`/`MarketCommand` render the exact line the request executes, from the same builder, credentials redacted (`redactArg`); every synchronous refusal returns `{error, command}`, and a failed package job too (the 202 answer and `GET /api/cli-jobs` via `cliJobView`). Both panes (`web/{browser,mobile}/src/components/GuestPackages.jsx`) show the vendor's words + **Copy command** (house clipboard pattern) + "Run it in a terminal."; the decision lives in `catalogRowAction`/`refusalCommand` (`web/shared/domain/cliPackages.js`), unit-tested.
Omp: `omp plugin discover` *is* the catalog — prose, `--json` accepted and ignored, prints `name@version` + a description line, never names the marketplace, keeps listing installed plugins; `plugin install <name>` without a marketplace resolves through npm and 404s. So `available: true`, `catalogInstall: false`, `catalogNeedsRoster: true`; `parseOmpDiscover` reads the prose and the pane shows the rows as information whose note states the `name@marketplace` form — no Install control (`testdata/omp.discover.txt`: real output, two marketplaces, one plugin installed).
OpenCode refuses that document itself ("Configuration is invalid … Expected array | undefined"), so listing the `plugin` string as a module was a lie; the read now fails loudly naming the file and the form the vendor expects.
Verified: `go test ./internal/clipkgs/...`; the gated live suite end to end in a fresh sandbox (16.4s), the new omp catalog assertions included; `make ci-scoped` PASS (22 paths); `make close` PASS, ff-ready, after three merges with a fast-moving main. Both panes read on a scratch instance, desktop and 390px mobile: Omp's Marketplace tab lists two rows with no Install control; a refused Disable shows Copy command titled `muse plugins disable picode-probe --json`; `__picodeOverlayAudit()` ok in every capture.
visual-review: PASS
Close-summary: 22 files changed, 648 insertions(+), 83 deletions(-); scope `FULL= GO=1 WEB=1 DOCS= METADATA=1`; in flight 3 ahead, 0 behind, clean, gates green just now; merge: fast-forward ready. Fragment `docs/changelog.d/pkgs-polish.md`; docs updated in `docs/architecture/cli-packages.md` and ADR-0167.

## Debts

- No Update action or availability badge for guests; Muse's built-in first-party plugins match no CLI command that reports them — `docs/handoff/open/packages.md`.
