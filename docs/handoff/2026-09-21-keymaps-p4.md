# 2026-09-21 — feat/keymaps-p4: OpenCode's key map, and Claude Code's gate settled

Shipped: **OpenCode's Keyboard tab**. 162 actions from the CLI's own Definitions
table at the tag the installed build pins (opencode 1.18.32), descriptions from
that table, written into the user `~/.config/opencode/tui.json`'s `keybinds`
object through the same flat engine — never the legacy `keybinds` section in
`opencode.json`, which the CLI's own startup migration moves into a `tui.json`
behind PiCode's back. A `.jsonc` sibling wins in the same directory and is the
file PiCode edits when present. Disabling is written the vendor's way (`"none"`;
`null` is invalid and drops the file's whole contribution), a disabled action
reads as unbound, and the scope is the user file: a project `tui.json`
deep-merges over the user's rows and wins, which the pane's file line states.
Pickup: restart, from the loader's own code (the config is snapshotted once when
the TUI starts; the SIGUSR2 reload RPC does not cover keybinds).

Also settled **live**, before any writing: Claude Code 2.1.278 logs
`KeybindingSetup initialized with 229 bindings` and `Watching for changes to
/home/goat/.claude/keybindings.json` — the customization feature is not gated
off on this build, and the pickup is hot-reload, observed. That answers the
material risk the research named (an employee-only gate would have made a
PiCode write a silent no-op).
Verified: `make ci-scoped` PASS; the engine's tests (162 rows from the vendor's
Definitions table minus the leader-key configuration and the object-form paste
binding, each noted in the catalog header; a write landing inside the existing
`keybinds` object beside `$schema` and unmanaged siblings; the vendor's "none"
reading as unbound; the `.jsonc` sibling winning; reset leaving siblings
untouched), the endpoint test (162 rows through the envelope, a write landing
as its own row), and `qa-cli-settings.mjs`, whose guest block now covers Omp,
Codex, Antigravity and OpenCode on both apps — OpenCode's captured chord landing
in its tui.json and its Reset all handing it back.
visual-review: PASS (six opencode frames read after the run — pristine, changed
and Reset-all on both apps; card 5/5 on both trios; the reviewer also caught
that the capture brief quoted wrong facet numbers — the pixels say All 162 /
Shared 32 / Off 43, with eight of APP's nine actions shipping disabled, which
matches the catalog).
Not done: **Claude Code's adapter is the last slice** — the inverted shape
(`bindings: [{context, bindings: {chord: action|null}}]`, 22 contexts, ~115
actions from the vendor's docs page) needs the inverted writer (context-block
splices, `null` as the unbind verb, a first write matching the vendor's own
`/keybindings` template) and its research is complete in
`docs/handoff/open/agent-clis-native.md`. Merge: fast-forward ready.

## Next up

- Claude Code's inverted adapter (the last row of the whole plan): the writer above, the catalog from the vendor's keybindings page, and the pane's context groups. Research complete in `docs/handoff/open/agent-clis-native.md`; gate settled live.
