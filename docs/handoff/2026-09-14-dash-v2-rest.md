# 2026-09-14 — dash-v2-rest: the attention line, and honest quota readings
Shipped: **attention line** above the KPI row (`DashboardView.jsx`) — Inbox
questions (the app's `Badge.Count` from `GET /api/apps`, read by `App.jsx`)
plus agent-CLI terminals whose hooks said `needs-you`, one action (`Open
Inbox` → `#/app/inbox`, or the waiting terminal). The selected agent's
`waiting` is not added: an agent asks through the Inbox, so both would count
one block twice. ADR-0109's 2026-09-14 amendment declares the door (precedent:
the phone's queue, ADR-0044). **Limits** (`LimitBars.jsx`) marks a reading
whose window already reset (`stale · reset 2d ago`, dimmed bar), carries its
age in the title and takes the coverage footnote the other panels have —
`limitReading` / `observedAge` in `dashboardStats.js`.
Verified: `make ci-scoped` PASS; 43 JS domain tests (four `limitReading` cases
+ `observedAge`); `tools/app-boundary.test.mjs` passes unchanged. Scratch
`qa-scratch.sh dv2` with two blocking items, a `needs-you` terminal and a
synthetic Codex rollout: line read back as `3 need you · 2 questions in Inbox ·
1 terminal waiting`, Limits showing `5h 42% resets in 2h` beside
`weekly 88% stale · reset 2d ago` + `Covers Codex only.`; with those cleared, no
line and `No agent CLI here reports a quota window.` +
`No CLI that ran records this.`; `__picodeOverlayAudit()` `ok: true`.
visual-review: PASS (var/screenshots/dv2-{attention,limits,empty,limits-empty}.png; card 5/5)

## Debts

- Only Codex reports quota windows; the rest have plan limits PiCode never
  reads, so `Limits` stays a one-CLI panel with a footnote.
