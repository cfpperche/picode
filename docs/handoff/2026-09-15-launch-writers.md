# 2026-09-15 — launch-writers: native Muse Code and Antigravity handoff writers

Fatia 4 of docs/plans/launch-muse-agy.md: native handoff writers for Muse Code and Antigravity (owner approved 2026-09-15); both GO, no brief-only fallback needed.
Muse spike: minimum proven is an index row plus a session log of plain records (metadata, user_intent.accepted, assistant_message_committed, tool batches); ladder gotchas: the --session argument must parse as UUID (all early ladder failures were arg parsing, not the store), approval records carry model as an object, and the exporter dispatches on the line prefix so schema_version must lead (Go maps serialize alphabetically — a struct keeps wire order). Permission frame deliberately absent: resume notes incomplete permission history and opens with defaults. Verified live: synthetic session exports, resume resolves it, same no-tty behavior as a real session.
Agy spike: summaries row plus brain transcript plus an existing store file (empty SQLite suffices, schema irrelevant); planted transcript answered from its own first prompt and continued a second turn; 4 tiny model turns spent total, all fakes deleted afterwards.
Shipped: muse_write.go and agy_write.go following the Grok file-writer ritual (create-only, round-trip re-read, cleanup on failure); tool traffic flattens to text where the format has no tool records; source model never becomes the new session's model. Tests: full no-binary round trip for agy, synthesis plus canned-export flow plus compat for muse, uuid-shape and refusal tests.
Verified on scratch: caps muse/agy {list,read,write,prompt}, preview pi->muse and pi->agy 200 modes [native,brief]. Actual commit-execute not run on scratch (needs seeded vendor stores); live proofs above cover it. Still missing: activity (Fatia 5). Deploy pending covers 3b+4.
visual-review: N/A (no rendered surface changed; verification is API + unit + live-binary probes)
