# 2026-09-15 — agy-sessions: Antigravity sessions from the conversation index

Task: second half of the owner's request (Muse Code sessions shipped in feat/muse-sessions).
Shipped: Antigravity indexes conversations in ~/.gemini/antigravity-cli/conversation_summaries.db (table conversation_summaries): conversation_id, title, preview, step_count, last_modified_time, workspace_uris (JSON array of file:// URIs), parent_conversation_id. AgySource (internal/clisession/agy.go) lists read-only and resumes with --conversation <id> (verified with agy --help, Antigravity 1.2.2). A row is listed only with steps > 0, a parseable workspace and a real time (Antigravity writes the year-1 zero time for unstarted conversations); nested conversations (parent_conversation_id) stay out. The per-conversation store at conversations/<id>.db provides the size column.
Gotcha: last_modified_time is declared 'datetime', so modernc.org/sqlite hands it back as RFC3339Nano (2026-09-15T13:49:14.466367991Z) while the file holds 2026-09-15 13:49:14.466367991+00:00 — agyTime parses both; the first version dropped every row because of it.
Verified: on a scratch with a copy of the real index, GET /api/clis/agy/sessions returned 4 conversations (the owner's 'Revisão Adversarial do Picode' 17 steps, 'Friendly Conversation Starter', 'Welcome To PiCode', 'Greeting And Language Inquiry' in /home/goat/tachyon); the pane shows Launch | Terminals | Sessions with the rows and 'Open in terminal'; the resume plan was verified read-only via POST /api/clis/agy/preview (args [--conversation, <id>], executable /home/goat/.local/bin/agy) — no terminal was created, so nothing landed in the owner's tmux. Tests: TestAgySourceList, TestAgySourceMissingAndLocked, TestAgyWorkspaceAndTime, registry.
Also fixed: the feed test (cli_terminal_feed_test.go) leaked one tmux session per run — its cleanup passed t.Context(), which Go cancels BEFORE cleanups run, so every tmux call failed silently; killFixture now uses its own context with a timeout and polls until the session is gone. The four leaked picode-sh-feed-fixture-* sessions and 19 orphan processes from removed worktrees were cleaned up by exact name/PID.
AGENTS.md: the tmux note now records the measurement (TMUX_TMPDIR ignored with and without XDG_RUNTIME_DIR) instead of a cause. Only Muse Code and Antigravity have no transcript reader yet (recorded in docs/handoff/open/agent-clis-native.md).
visual-review: PASS (var/screenshots/agy-sessions.png; card 5/5)
