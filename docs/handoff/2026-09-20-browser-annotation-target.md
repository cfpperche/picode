# Browser annotation destination

- Independent browser tabs now list running terminal destinations and never fall back to the first running terminal.
- Bound browser panes show that annotations go to the current session.
- Send remains disabled until an independent tab has a selected destination.
- Validation: `npm run build` and the web workspace test suite passed (854 tests).
