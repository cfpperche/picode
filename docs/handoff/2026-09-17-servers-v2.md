# 2026-09-17 — feat/servers-v2: the Servers panel names a port and can stop it

Shipped: the Servers panel v2 (ADR-0151). The read (`GET /api/devservers`)
now reports `kind` (page / api / opaque), `scheme`, the process that holds the
socket (`pid`, `startKey`, `startedAt`), `state`, saved hides and `readable`;
the row decision needs the port to be listening at this read unless a PiCode
pane still owns it. Three writes: `POST /api/devservers/stop` (SIGTERM, then
SIGKILL on a forced second request, only for a process re-derived inside a
PiCode pane with pid + start token matched at action time), `/hide` and
`/unhide` (keyed by port + pid + start token; pruned when the process is gone).
The row is a container: name/URL open when the port is a page, and the menu
carries Copy address, Show terminal, Stop server…, Hide. Design + decision
tables: `docs/plans/servers-panel-v2.md`.

Verified: `make ci-scoped` PASS (fmt, vet, hooks, go, test-js, build, docs).
Go tests cover the classification table, the visibility rules, every Stop
refusal row and a real child that ends on SIGTERM/SIGKILL. Scratch QA
(`scripts/qa-scratch.sh`, fixtures: page/API/silent listeners in PiCode
terminals, plus page and API started outside): screenshots read for the
default panel, the row menu, the stop confirmation, the hidden section,
"Show N more" and the empty state; `__picodeOverlayAudit()` ok on each frame;
the stop proved at the process level (the owning pane printed `Terminated`).
Method's blind spot: the blocked line for a daemon without `/proc` is covered
by `TestDevServerUnreadableOwners`, not visually — this machine is Linux, and
the desktop shell was not exercised on Windows.

visual-review: PASS (8 screenshots read: default, menu, confirm, after-stop,
hidden, show-more, empty, after-reload; overlayAudit ok, no console errors)

Merge: fast-forward ready.

## Next up

- Windows shell check: the row menu, Stop and the hidden section inside the
  desktop app (the frame/webview path is unchanged, the row is not).

## Debts

- The Servers panel has no public capture: `app-inspector` photographs the Changes tab, so nothing was stale (measured 2026-09-17: `make docs-shots` changed only `app-canvas`).
