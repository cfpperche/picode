# 2026-09-12 — communication-recovery: harden native recovery
Implemented: preserve live hooks without Linux process metadata; nonblocking shared fence locks distinguish active writes from persistent recorder failures.
Implemented: failed recording offers Terminal controls on desktop/mobile; uncertain delivery logs bounded reasons without message or terminal content; no automatic retry.
Implemented: native launchers override inherited CLI markers; missing own identities cannot borrow a parent Codex connection.
Reviewed: current-main native identity, Pi reply routing, consent audit and approval safeguards retained; adversarial review PASS.
Verified: `make ci-scoped` PASS (full); `make close` PASS after marker fix; 32 decision rows covered by regressions or recorded native evidence/limits.
Verified: scratch desktop light/dark and mobile screenshot review PASS; both confirmation overlay audits clean.
visual-review: PASS — root `var/screenshots/communication-recovery-20260912/`.
Native: abrupt restart recovered all six identities/Idle in 0.405s after healthy (startup 0.709s), with identical PID/run/session checkpoints and the Codex unsent draft preserved.
Native: Claude → Hermes and Grok → Codex passed with matching reply IDs and both ACKs, independently checked in `message-receipts.json`.
Limits: four connected; Pi OAuth invalid_grant/adapter reconnect and OpenCode Z.AI balance/wrapped-footer draft guard prevented a six-CLI transport pass. Pre-fix Codex → Hermes expired.
Evidence: root `var/qa/communication-recovery-20260912/`; September 10 eight passed checks remain separate historical evidence.
Cleanup: six exact fixture terminals and scratch daemon stopped; orphan cleanup restricted to verified scratch HOME.
Not done / debts: main full CI follows fast-forward; physical-mobile/non-Linux runtime acceptance and blocked Pi/OpenCode exchanges remain open.
Merge: fast-forward ready after closing gates. No deployment performed.
