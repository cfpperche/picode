# 2026-09-13 — tmux app (`feat/tmux-app`)

**Shipped (branch, not merged).** The tmux app: `GET /api/tmux/server` +
`POST /api/tmux/sessions/{name}/reap` (`internal/server/tmux.go`), the
server-wide read in a new file `internal/tmux/server.go` (one `list-panes -a`,
one row per session, markers as the ownership receipt), the native-surface app
(`internal/apps/tmux.go`, no badge), the desktop body
(`web/browser/src/components/tmux/TmuxSurface.jsx` + `styles/tmux.css`, lazy
chunk, icon key `tmux`), and the docs: ADR-0133, `docs/architecture/tmux-app.md`,
`docs/plans/tmux-app.md`, `docs-site/guide/tmux.md`, changelog fragment.

**Verified.** 11 new tests in `internal/tmux` + `internal/server` (the reap
decision table row by row, the absence list, the marker/constant pin, the
one-call invariant); `make test-js` 13 suites pass; `make web` and `ci-docs`
pass (OpenAPI regenerated, 55 lines); `app-boundary.test.mjs` gained the second
native-app row. Live against a scratch instance: the read attributed 1 session
to the scratch's terminal and 15 to "no record" on the shared socket. Visual
review PASS with four read screenshots in `var/screenshots/` (sessions,
expanded leftover, the confirm overlay, Server, filtered-empty); `overlayAudit`
ok on each. Two defects found by reading the pixels and fixed: a placeholder
truncated mid-word ("…commands, folde") and 15 red Remove buttons repeating
down the list (the action moved inside the row's reveal).

**BLOCKED — the branch cannot be green, and neither can anyone's.**
`make ci` is red on `main` already: `TestPeerStopStubbornChildStaysPending`
is the line `docs/handoff/2026-09-13-main-red-peers.md` recorded (whoever
lands first owns it), and `TestCLITerminalResumeDecisionTable` is a second
one it did not name — deterministic on a clean `02d9f792`, now recorded in
`docs/handoff/open/terminal.md`. `TestPaneRootSurvivesSIGHUP` failed in the
shards here and passes alone (load), matching that note. Merge and CI are
the owner's call until those lines are resolved.

**Debts.** No test drives a real tmux server (every test scripts the seam —
deliberate, so the suite cannot strand sessions on the developer's own);
`PICODE_TERM_ID` has no store constant (pinned by a literal in the test);
the absence list is covered by unit test but its populated state was not
captured in the browser; the confirm dialog's danger button does not read as
destructive in the shell's dialog (not this app's component).

**Merged main.** See `git log`.
