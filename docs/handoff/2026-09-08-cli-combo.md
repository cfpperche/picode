# 2026-09-08 — feat/cli-combo: Agent CLI favicon combobox
Shipped: Settings, Sessions and the new-terminal form share `CliCombo` (SearchCombo + TerminalCliBadge) at `--ctl-h` instead of an unstyled native select. Desktop and mobile.
Verified: `make close` green; scratch `qa-scratch.sh` :8471 — settings closed/open, sessions empty + open (all six CLIs), unsupported settings notice, launch form open. overlayAudit ok; row heights 36px.
visual-review: PASS (cli-combo-sessions-open.png + overlayAudit ok; card 5/5)
Not done / debts: Settings still lists only Pi (CLI_SETTINGS); Auto-clean stays a native select.
Merge: fast-forward ready.
