# 2026-09-08 — feat/pins-v2: pins hygiene slice + reminders plan (proposed)
Shipped: slice 1 of `docs/plans/pins-v2.md` — the 16-finding adversarial
review's fixes: rune-safe limits that refuse (ErrInvalid → 400, ErrConflict
→ 409, one `pinStatus`), summary-only list and `pin.*` events, annotate by
reference (`bg:` scene files stripped, restored from `baseFileId`), atomic
sketch writes with rollback, `?v=` preview URLs (migration 035 adds
`pin_files.updated_at`), PATCH precondition + Reload notice, retained
drafts in sessionStorage, auto-title from the first file + Cancel offers
delete, sidebar on the feed, RFC 6266 names, boot sweep of orphan dirs,
`notify()` normalizes raw notices (both twins). Docs: study
`docs/benchmarks/2026-09-08-pins-reminders.md`, plan, ADR-0100 (proposed).
Why: a byte-sliced title left invalid UTF-8 in SQLite; annotating >1.5 MB
images failed with the wrong message; edited sketches showed stale
thumbnails; 105 KB events per save. Owner approved slice 1 in chat.
Verified: `make ci-scoped` PASS; store + server tests (limits, conflict,
event shape, sketch round-trip, sweep); node tests for pinDraft; scratch
QA on :8471 — 6 MB annotate saves and reopens with the picture, re-save
moves the preview version, 409 card + Reload, draft restored after a
detour, sidebar shows an API-created pin live, "95 KB / 100 KB" counter,
drop-on-new names the pin "big" and Cancel offers Delete (screenshots in
the session scratchpad only).
Not done / debts: mobile read-only pin viewer (slice 1's one carry-over;
`#/pins/<id>` still lands on Settings on the phone); slices 2–4 wait for
the owner (ADR renumbered 0099 → 0100 at merge; reminders migration is
now 036). CHANGELOG had committed conflict markers from f7c91834 — resolved
as the union of both sides here.
Merge: fast-forward ready after `make close`.
