# 2026-09-22 — feat/ade-boundary

Shipped: ADR-0179 (proposed, direction approved by the owner) — agent CLIs are optional and tmux is the only runtime dependency. It supersedes ADR-0003's "requires `pi` on PATH, checked at startup" (its never-vendor half stands) and amends ADR-0045 (automations ask for Pi only when the run is a Pi process), ADR-0050 (the provision doctor reports which CLIs are present), ADR-0098 (the Windows installer's runtime is tmux, git, curl, Node 22 and npm) and ADR-0160 (free agents too). The front doors now describe a multi-CLI ADE: the AGENTS.md scope paragraph ("Pi is one CLI among nine, never a dependency"), the README, the docs landing page, Getting started (tmux and PiCode first, CLIs afterwards from Agent CLIs), from-source, the remote/shared/public-server pages, the `llms.txt` intro, the `picode --help` banner and the systemd unit's `Description=`. The plan with the 2026-09-22 inventory is `docs/plans/multi-cli-ade.md`; the changelog fragment is `docs/changelog.d/ade-boundary.md`.

Verified: `make ci-scoped` PASS — fmt, vet, hooks, go, test-js, docs; Vale 0 errors. Four commits on top of `main` (b335ef7f), fast-forward ready.

Not done, on purpose: this branch changed words and the decision, not the code that still contradicts them. The System page (`/api/system`) still prints a fixed `pi` block and the npm warning; the automations gate still asks `LookPath("pi")` for every run; `picode provision`'s `piStep` still blocks and exits 1 without Pi; the Windows installer still lists `pi` in `RuntimeTools` and reads the Node major from Pi's package; a free agent is still always Pi. Until the next four branches land, a machine without Pi is described correctly by the docs and still warned at by the product — `docs-site/guide/remote-server.md` says so in one caveat sentence, and `docs/architecture.md` still lists `pi` on PATH among the doctor's server steps because that is what the doctor does today. Those branches, their order and the debts they leave (installer run on the `picode-test` VM, backup scope, What's new headline) live in `docs/handoff/open/multi-cli-ade.md`; nothing is repeated here.

Owner-owned: the GitHub repository description and topics still say "for Pi coding agents" (`gh repo edit`), tracked in the same topic file.
