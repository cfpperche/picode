# 2026-09-25 — one-refresh-owner

Pays the pi/Claude Code debt from `anthropic-usage`: Use refuses a row whose
refresh token another CLI holds live (both Use routes, pi included) and the
pane offers "Sign in to <CLI>"; a share made earlier shows "shared with …".
ADR-0166 amendment 2026-09-25; `docs/architecture/credentials.md`.

- On the owner's machine today Pi and Claude Code hold different Anthropic
  logins (checked by comparison only), so nothing was shared.
- QA on scratch `oneowner`: the chip on a shared row and the refusal dialog.
