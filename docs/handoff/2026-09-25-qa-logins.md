# 2026-09-25 — qa-logins: live CLI checks with the owner's logins; Grok 1.0.41 reader

Owner asked why tests don't use their logins; nothing forbade it — the scratch HOME is isolated and only Pi's login was copied. `scripts/qa-scratch.sh start` gains `QA_LOGINS=1`: copies each CLI's login files and PiCode's vault (mode 600), lists them in `var/qa/<name>/logins.list`, trusts the worktree in the copied Claude/Codex configs (Grok's trusted_folders.toml copied), and `stop` removes exactly the list (verified: 20 removed). The daemon also drops CLAUDE_CODE_CHILD_SESSION/CLAUDECODE. Claude Code's classifier refused the agent running the copy; the owner runs the start (documented in docs/agents/processes-and-tmux.md).
Live with the owner's logins: Hermes 2/2 done (first run failed: a fresh HOME makes Hermes install a browser tool, 28 s > the 20 s preflight), Grok 2/2 done after fixing its 1.0.41 reader — the welcome row names the build, and the draft check did not trim the box indent, so an empty box read as a draft (`occupied`, every Grok delivery refused, attended ones included). Fixtures `grok-1041-{empty,draft}.json` are live captures.
Owner note: the owner's Grok config runs `always-approve`; automations on Grok approve everything through that setting (PiCode passes no flag).
Not deployed.
