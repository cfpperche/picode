# 2026-09-17 — omp-cli: Omp joins the catalog as the terminal-only row

Shipped: catalog row `omp` (surface terminal) — PATH detection, Check setup
(`omp --version`, needs Bun ≥ 1.3.14), New terminal; identity mark `Om` with
the vendor favicon, read-only Launch preview, no Sessions tab, no Activity
toggle, seed skips the row, update-check answers the honest "no managed
lifecycle". Probe findings and the six-slice plan live in
`docs/plans/omp-cli.md`; docs updated (architecture, cli-terminal-launch,
agent-clis guide, AGENTS.md CLI list).
Verified: `make close` green (fmt,vet,go[20],test-js,build,docs); scratch
`ompa` desktop card read in both states — checked (`omp/18.2.4` with
Bun 1.3.14 on the daemon PATH) and the honest failure under the machine's
Bun 1.3.10; `overlayAudit` ok on both.
visual-review: PASS (omp-card-checked.png, omp-check-oldbun.png; card 5/5)
Not done / debts: sessions, launch wrapper, activity, lifecycle and handoff
are Fatias 2–6, each probe-gated (plan doc). omp was installed globally via
npm on this machine for the probe; with the system Bun 1.3.10 its Check
setup fails until Bun ≥ 1.3.14 reaches the user's PATH.
Merge: fast-forward ready.

## Next up

- Omp Fatia 2 (sessions): gate is one real omp session on disk (needs auth); `ResumeArgs: ["--resume", id]` per its `--help`. Plan: `docs/plans/omp-cli.md`.

## Debts

- omp needs Bun ≥ 1.3.14 on the user's PATH; this machine's 1.3.10 fails Check setup with the bundle's SyntaxError (measured 2026-09-17).
