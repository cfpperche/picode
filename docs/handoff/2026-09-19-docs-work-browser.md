# 2026-09-19 — the browser gets its architecture file (docs-work-browser)

Inventory work after the owner asked what is still pending: two debts of the
browser topic were cheap and are paid here.

## What landed
- `docs/architecture/work-browser.md` (the debt said "worth one file the next
  time a browser slice lands" — one just had): who decides what, the command
  channel line, tabs and parking, the overlay geometry rule, the permission
  matrix, the annotation rules that are load-bearing (the pull door, the
  hidden overlay, `styles0`/`inline0`, one Send = one package), evidence per
  layer, and the architectural traps. Index row added to
  `docs/architecture.md`.
- `desktop-shell/permissions/autogenerated/btab_layer.toml` deleted — stale
  since the command left `build.rs`; the schema entries go with it on the next
  build (verified by the cross-build's regenerated `gen/schemas/*`).

## Next up
- **The batch Send cap (owner decision)**: the prompt door takes 4 files per
  call and one note stages 2 (note + crop), so 3+ pins Send visibly fails
  ("Annotations saved, but not sent: at most 4 files") with the notes left in
  the store. Shapes on the table: chunked pastes (N pastes of ≤4), one
  combined note (1 + N files), or the crops referenced from one note (1 file
  whichever N). Measured, not guessed: `checkPromptPaths`, `term_prompt_test.go:59`.

## Debts
- None new. The rest of the browser topic's debts are unchanged (Ask never run
  off Windows, device toolbar, cookies/passwords import, ADR-0143 wire rows).
