# 2026-09-10 — focus-mode: a fullscreen mode for the desktop shell

Shipped: one shell-wide, per-viewer fullscreen mode (`picode-focus`, `picode-focus-seen`) that takes the sidebar, tab strip and Inspector rail out of the layout and gives the selected tab the whole window; three 6px edge strips bring one panel back as an overlay (120 ms dwell in, 250 ms out), the top one carrying a **Leave fullscreen** control. Decision table in `web/shared/domain/focusMode.js` (28 node tests); DOM half in `web/desktop/src/lib/useFocusMode.js`; strips and control in `components/FocusEdges.jsx`. Rows in the generic and terminal context menus, the palette, and `app.fullscreen.toggle` (`Ctrl+Shift+Enter` / `Cmd+Shift+Enter`). Docs: a paragraph in `docs/architecture/routes.md`, one in `docs-site/guide/getting-started.md`, `docs/changelog.d/focus-mode.md`. No ADR: a UI refinement crossing no boundary (ADR-0105).
Judgment call for the owner: entering also calls `requestFullscreen()` — "janela full" read literally. The owner described app chrome only. It costs the double-Escape (below); accept or revert before merging.
Verified: `make ci-scoped` PASS at every step, `make close` green, 55 JS tests over the three touched modules. Browser QA on a `scripts/qa-scratch.sh` instance across a terminal tab, the Matrix app tab and a file tab: every decision-table row, `__picodeOverlayAudit()` ok with each of the four menus open, xterm 121x52 → 188x54 on entry and back, Matrix canvas 1020 → 1584 px with attaches steady at 1, a stubbed `requestFullscreen` rejection, a reload, a narrowed window and a page-route navigation.
visual-review: PASS (16 captures read in a subagent over three rounds; revealed rail matches its docked control row for row, 34 label + 33 count bands at identical y in both themes; toast 356x58; all four menus on one 304px grid). Fixed in-session from the earlier rounds: the revealed panel was invisible against a near-black terminal (1.08:1) — it now carries a 2px accent seam, 5.97:1 dark / 4.13:1 light; the Leave control read as a label — it now has a surface and its chord; the strip's seam stacked into a stripe under the active tab — it recolours its own border instead.
Not done / debts: in real browser fullscreen the browser owns Escape, so one press leaves the mode — the double-Escape (reveal first) only happens in the in-app mode, after a reload or a refusal; a terminal guest keeps Escape too. The right strip is pointer-transparent so surface scrollbars keep their grab area, so its reveal misses an embedded frame (the PDF preview). Chromium only; the pointer dwell has no touch equivalent. A revealed panel covers the top or left of the surface under it while open — by design, since it must not resize a pane.
Merge: fast-forward ready on `a4f07cfa`.
