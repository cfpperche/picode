# 2026-09-20 — agent-row-v2: mobile agent cards reach term-row parity
Owner clarification on the Work-tab refactor: the gap was the AgentRow
itself — state folded to Stopped/Idle with no activity and a bare
start/stop square. AgentRow now renders the same shape as TermRow: the
chip reads the bound terminal through agentState/agentRowStatus
(Working · age via terminalActivityStamp, Needs you, Open, Stopped) and
the edge button opens the shared agentRowMenu (lifecycle, open
chat/terminal, launch settings, Continue in… handoff, rename via PATCH
/api/agents/{id}, remove with 404 tolerance), wired through a new
onAgentAction in mobile App.jsx. A legacy interactive Pi pane drops the
restart item (no terminal to restart) instead of offering a no-op.
Verified on the scratch (390-content at desktop width): menu renders,
Escape closes it, Start flipped Atlas Stopped→Idle, Rename persisted
("Atlas QA"), overlay audit ok:true, screenshots read
(var/screenshots/agent-row-v2-*.png). visual-review: PASS

## Debts

- Needs-you/Working chip states were not exercised live on the scratch
  (no working CLI agent at QA time); the vocabulary is the shared
  agentStatus fold the terminal row already renders.
