# 2026-09-08 — feat/pins-list-v2: pins v2 slice 4, search / keep on top / archive
Shipped: migration 037 (`pins.starred`, `pins.archived_at`);
`ListPins(PinListFilter{Q, Archived})` — word-wise LIKE over title, tags,
body with escaping and `lower()`, starred first, live before archived;
`SetPinStarred`, `SetPinArchived` (closes open reminder items),
`CountArchivedPins`; `DueReminderIDs` skips archived pins; routes
`GET /api/pins?q=|?archived=1` (+ `archived` count), `POST
/api/pins/{id}/starred|archived`. Sidebar: search box (150 ms debounce,
stale answers dropped, Escape clears), star/archive/delete on the card,
"N archived" view with Unarchive; studio: Archive / Keep on top beside
Delete, "Archived · reminders paused" note; mobile pin screen chips.
Why: owner approved slice 4 in chat, 2026-09-08 — closes the pins v2 plan.
Verified: store test (order, archive in/out of list and search, LIKE
metacharacters, non-ASCII, reminders paused/resumed, item closed on
archive), server routes, `ci-scoped` PASS; scratch :8475 — star reorders
live, archive removes the card and shows "1 archived", search "archive"
finds the archived pin flagged, "smoke release" needs both words, the
archived view lists and Unarchive returns it, studio buttons toggle and
the sidebar follows through the feed.
Not done / debts: search is LIKE, not FTS (fine for hundreds of pins;
revisit with FTS5 if it ever drags); the `fill ""` of agent-browser does
not clear an input — use Escape/select-all in QA batches; the pins v2
plan is complete, remaining ideas (checklists in the body, multiple
reminders per pin) have no owner ask.
Merge: fast-forward ready after `make close`.
