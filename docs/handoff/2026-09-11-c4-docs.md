# 2026-09-11 — c4-docs (C4: docs for package config)

Closes the C4 row of `docs/plans/package-config-manifest.md`.

- `docs-site/guide/packages.md`: the "Package links and configuration"
  section became two — **Package links** (unchanged facts) and
  **Configure a package** (descriptor-driven form, file stays the
  package's own, unknown keys preserved, broken file never silently
  replaced, machine vs workspace scope in plain words) plus
  **Describe a package yourself** (describe → configure → edit → delete;
  precedence user > package > catalog).
- `docs/architecture/cli-packages.md`: the Configuration paragraph now
  states resolution order (user → manifest → catalog), typed fields
  (tri-state boolean, numeric min/max), source named on the page, and
  the describe/edit/delete lifecycle.
- `docs/architecture/model-roles.md`: one clause — ADR-0119 leaves the
  bespoke roles editor in place; descriptors are additive.
- Changelog fragment gains the docs line. handoff.md item 3 updated.

Gates: `make close` green on `feat/c4-docs`; fast-forward to `main` and
`make ci` from the root.
