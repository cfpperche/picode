# 2026-09-11 — feat/wsl-review2: CanSparse was false on every real machine

Shipped: the first live run of the merged `picode-desktop disk` (after the
owner's deploy) showed no `WSL … can convert it` line — `wsl.exe --help` exits
**255** while printing the full text to stdout, and `DistroDisk` dropped output
on a non-zero exit, so `CanSparse` was false everywhere real. The guard is now
`err == nil || len(out) > 0` for `--help` and `--version`, with a regression
test fed the captured behaviour (internal/desktop/disk_test.go).

Verified live after the deploy: the merged report — C: 31 GB free, 218 GB VHDX
not sparse, held ≈92 GB, distro 126 GB used, 43 GB reclaimable (32 GB go-build).
`make ci-scoped` gates; `make close`.

visual-review: n/a — tray/report are text surfaces; wording covered by
`diskLine` tests.

Debts: unchanged — warning is words only (no alert icon asset); docker's
storage belongs to the Docker app. Next: P2 Storage app + the Windows-facts
route (needs its ADR), P3 the actions that stop the distro
(`docs/plans/wsl-control.md`).

Merge: fast-forward ready.
