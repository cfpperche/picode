# 2026-09-20 — feat/codex-git: a memory store's own git history, and the pane's first row
Shipped: the owner asked for two things — Codex's git, and the first row (workspace/scope buttons) glued to the tab bar. (1) `internal/climemory/gitstore.go`: a memory store the CLI keeps under version control reports its own history — Codex is the one that does, `~/.codex/memories` being a git repo whose first commit is the baseline Codex wrote. `Survey` now returns a `climemory.Audit` carrying `History{Revisions, LastAt, Subject, Changed}` instead of a bare `IndexHealth`, surfaced as `history` on `GET /api/cli-memory`. (2) `modified` precedence is now `memory` (the CLI's own frontmatter date) > `git` (the revision that last changed the file) > `file` (mtime, and any file with uncommitted changes); measured reason: all three Codex files reported "today" from their mtime while their content had not changed since the 12:10 baseline. (3) Only a `.git` directly in the store folder counts — Muse keeps memory in `<workspace>/.agents/memory` inside the user's own repository, and walking up would report the project's history as the memory's; `TestHistoryIsTheStoresOwnOrNothing` holds that line. (4) Bounds: 3s timeout, at most 500 revisions walked, `GIT_OPTIONAL_LOCKS=0` so a read never takes the index lock while the CLI writes, any failure reports no history rather than half of one, and `git status -z` keeps a filename with a space from being parsed into a file wearing a revision date it no longer matches. (5) Layout: the pane now carries its own `padding-top` (12px, 8px under 720px) — every sibling view got it from a section id and Memory had none, which is why the first row sat flush against the tab bar. The toolbar is one cluster anchored left (the Providers roster's house rule) instead of `space-between` pushing the filter to the far right against an empty spacer, and it is absent entirely when there is nothing to switch and nothing to filter; on the phone the store switcher keeps its own line and the filter joins the sort/Select row. (6) A read-only CLI now names its clear command inside the sentence with a plain `Copy` button — it used to read "Copy grok memory clear". (7) New pure helper `historyLine()` in `web/shared/domain/memoryTable.js`, shared by both apps, with tests.
Verified: `make close` exit 0 — `ci-scoped: PASS (fmt,vet,hooks,go[5],test-js,build; 10 paths)`; 11 tests pass in `memoryTable.js`. Go tests added: `TestHistoryReadsTheStoresOwnRepository` (baseline revision, clean tree, every row dated from git; then one file rewritten → it is named in `Changed` and falls back to the filesystem) and `TestHistoryIsTheStoresOwnOrNothing` (two subtests: a memory folder inside the user's repo reports nothing; the CLI's own frontmatter date still outranks the revision) — both skip when git is not installed. visual-review PASS on scratch `qa-scratch.sh start cgit` (:8473), seeded with copies of the real Codex store (git repo and all), the real Grok store and the real Claude Code store, plus a deliberate edit to `raw_memories.md` so the drifted state was exercised: cgit-codex.png (clean history line), cgit-codex-drifted.png (light mode, warning colour, "1 file has changed since"), cgit-grok.png (scope switcher + filter in one left cluster, the new Copy notice), cgit-claude.png (table unchanged, toolbar moved), cgit-phone-grok.png and cgit-phone-codex.png at 390x844. Measured: 12px desktop / 8px phone from the tab bar's bottom to the pane's first child; no horizontal overflow at 390px (scrollWidth 390 = clientWidth); `window.__picodeOverlayAudit()` ok:true on every capture. No ADR: additive to ADR-0163's pane, crossing no protocol, persistence, security-model or process boundary.
visual-review: PASS
Merge: one commit (79d9eb95 on base 97f50a21), 12 files, +606/-74. `main` had moved 10 commits at close-summary time, so the landing rite — merge `main`, re-run `make close`, then fast-forward — still follows this note.

## Debts

- History is read by shelling out to `git`, so a store on a machine without git in PATH silently reports no history; the tests skip in that case rather than cover it. The pane's other open items stay in `docs/handoff/open/agent-clis-native.md`.
