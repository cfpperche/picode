# 2026-09-20 — feat/memory-table: the Memory pane is a table that audits the store

Shipped: slices 1-3 of `docs/plans/memory-table.md` (owner-approved, no new dependency). (1) `internal/climemory/survey.go` reads the folder instead of one file at a time: `indexed` (does MEMORY.md still name this file), `citedBy` (inbound wikilinks), `broken` (its own links with no file behind them), `index.dangling`, and the index's lines/bytes against the 200-line / 25 KB budget Claude Code actually reads. Pointer fields, so a CLI with no index convention reports absence rather than a zero that reads as a finding; 32 KB scan cap per file. (2) The desktop pane is now a real `<table>` (table-layout: fixed) with sortable headers carrying aria-sort, kind facets with counts, the index budget meter, and the index row pinned first in every sort; under 720px the two number columns hide and Health halves. (3) Multi-select and bulk delete whose confirm names the blast radius (links broken, index rows left dangling); a partial delete reports how far it got. The phone app keeps a list and shows the same numbers as a meta line under the title with a native Sort select (ADR-0072); both apps share the pure logic in `web/shared/domain/memoryTable.js` (10 tests). Also dropped Codex's `clear: "codex"` — the read-only notice offered it as "the vendor's own command" and `codex` alone does nothing to a memory. `originSessionId` is in the payload but deliberately not a column: PiCode has no route that opens a Claude Code session from its id. No ADR — the change is additive to ADR-0163's pane and crosses no protocol, persistence, security-model or process boundary.

Verified: `make close` exit 0, `ci-scoped: PASS (fmt,vet,hooks,go[5],test-js,build; 16 paths)`. Measured against this repository's own Claude Code store (54 memories) and reproduced exactly by the shipped code: 1 broken link (`no-typing-into-other-sessions` -> `[[picode-parallel-sessions]]`), 23 cited by nothing, most cited `picode-scratch-dogfood` (10), index 54 lines / 10630 bytes = 41% of the byte budget.

visual-review: PASS — scratch instance (`qa-scratch.sh start memtable`, :8473) seeded with a copy of the real memory corpus plus one deliberately unindexed file and one deleted file, so "Not indexed", "N broken links" and the dangling-index state were all exercised. Screenshots read: mem-table-desktop, -light, -dark-bulk, -row-open, -narrow (700px), -filter-empty, mem-codex (read-only + empty), mem-hermes (empty), mem-phone, -list, -bulk (390x844). `window.__picodeOverlayAudit()` ok:true on desktop and phone after fixing the bulk bar. Three defects the screenshots found and the session fixed, all of the kind a green suite does not catch: auto table layout pushed every number column off the right edge; title and description sharing one line truncated the description to meaninglessness, so the cell now stacks; the bulk bar failed the `[data-align-row]` audit until the blast-radius sentence moved out of the control row.

Merge: fast-forward ready (2 commits, 16 files, +1510/-108; base dc7c4225, head 85aafa0a). The open debts from this work, and slice 4 (cross-project view, TanStack v9, virtualization — the only slice that would add a dependency), are already recorded in `docs/handoff/open/agent-clis-native.md`.
