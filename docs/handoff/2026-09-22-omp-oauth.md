# 2026-09-22 — feat/omp-oauth: guided browser OAuth for omp's five oauth providers
Shipped: the Add provider dialog's Guided sign-in now drives browser OAuth for omp's five oauth
providers (anthropic, openai-codex, github-copilot, kimi-coding, xai): the authorize page opens in
a tab, the loopback/device callback returns to PiCode, and the minted credential lands in the
VAULT (oauth.StartSink + ompVaultSink); omp reads it through its declared env names at spawn
(ompCredentialEnv bridge in cli_launch.go). ADR-0178 supersedes ADR-0168's client-id clause
(owner-approved); the signin route accepts provider and the terminal strip stays the fallback.
Files: internal/oauth/{oauth.go,device.go}, internal/server/{credentials.go,cli_launch.go,omp_oauth_test.go}, web/{browser,mobile}/CliCredentials.jsx; docs: 0178 (+index), cli-providers.md, open topic, fragment.
Verified: scoped CI green (fmt, vet, hooks, go[5], test-js, build); flow exercised in the scratch instance. Blind spot: full `make ci` not run.
visual-review: PASS (dialog visuals PASSed in the previous round)
Not done / debts: omp agent.db Native reader — owned by docs/handoff/open/omp-oauth-parity.md.
Merge: fast-forward ready.

## Next up
- Commit the working-tree edits (oauth/server Go files + test, doc edits, fragment) before merge.

## Debts
- PiCode reads omp `agent.db` (Native reader; ADR first) — owned by docs/handoff/open/omp-oauth-parity.md.

Functional browser check: authorize URL + PKCE verified via window.open spy in the scratch instance.
