# 2026-09-22 — feat/omp-roles: omp model roles, fallback chains and cycle in Settings

Shipped: omp's Settings pane (`#/clis/omp/settings`) edits model roles (15 built-in + roles named by the files), fallback chains (`retry.fallbackChains`) and the quick-switch cycle (`cycleOrder`) on both layers — including the workspace layer `omp config set` cannot write. ADR-0181 (proposed, amends ADR-0163): field kinds `role` and `list`; `spliceList` is the single implementation behind the key-map writer's SetStrings. New `GET /api/cli-models?cli=omp&workspace=` (`internal/climodels`) runs `omp models --json --kind all` in the workspace, lazily when the picker opens. New scalar rows: modelRoleStorage, defaultThinkingLevel, eight `retry.*` knobs. The placement study `docs/benchmarks/2026-09-22-omp-helpers-placement.md` lands with it (§10 recommendation, §11 five measurements).

Fixed (found by driving real saves): the guest CLI settings pane sent a raw object to fetch, so every guest CLI settings save since 2026-09-20 answered "invalid request body" — now JSON-encoded; `yamlInsert` recreated a nested key's whole chain from the root when only the middle was missing (duplicate block); a value with a line break is refused instead of written as a YAML block.

Verified: go tests (clisettings role subtests, climodels, server), test-js, `make ci-scoped` PASS, `make close` PASS before the merge of main. Every write path driven through the real UI on a qa-scratch with an isolated HOME (pick a model, thinking level, Use inherited, chain entry, new role via tag, new fallback), file read back each time. Blind spot: never read by a live omp TUI (pickup is restart, measured separately); picker populated only with a dummy OpenAI key.
visual-review: PASS (two fix rounds; desktop 1600×1000 and 1280×633, mobile 390×844, dark; overlayAudit ok; final 07-final-roles.png)

Not done / debts: flat dotted keys (`composer.shape: band`) read as "not set" and a save adds a nested duplicate — omp does the same; durable home is `docs/handoff/open/agent-clis-native.md`. The IQ column is not in `omp models --json`. Visual nits: DEFAULT help line close to the picker; picker chevron sits after its text while select chevrons sit at the edge.
Merge: fast-forward ready after `make close` reruns on the merged branch.

## Next up

- Slice 2: Models tab in Providers reusing `/api/cli-models`.
- Slice 4: resolved-config + doctor card from `omp config list --json`.

## Debts

- Flat dotted-key files read as unset and get a nested duplicate on save — needs a flat-form fixture and a doctor finding (`docs/handoff/open/agent-clis-native.md`).
