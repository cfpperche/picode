# 2026-09-20 — creds-bar: the Providers bar's buttons, one group (owner's screenshot)

Shipped: `.cred-bar[data-align-row]` puts its controls in one `.cred-bar-actions`
group with the single `margin-left:auto` (count left, buttons together at the
right edge, 8px gap, all `--ctl-h`); the bar-level **Add API key** renders only
when the pane has one provider, so a multi-provider pane (Omp, 5 groups) no
longer shows a bar Add directly above the first provider's own. Both apps.

Verified: ci-scoped PASS (fmt,vet,hooks,test-js,build); scratch QA at
1560×900 and 414×896 measured the group's right edge == the bar's right edge
(1469==1469, 384==384), buttons on one row at 36px, no horizontal overflow at
414px, and exactly one Add per provider group in the multi-provider pane.
Screenshots read by a subagent (BarRead): Omp desktop/mobile + Claude Code
desktop all ok; card 5/5; the floating Sign in and the duplicate Add are gone
in pixels. Blind spot: hover/scroll affordances not provable from stills.

visual-review: PASS (creds-bar-omp.png + creds-bar-claude.png + creds-bar-mobile.png; card 5/5)
Not done / debts: the bar's buttons are a smaller size class than the pane
header's (29px vs 44px) — pre-existing and consistent across the app, not a
regression from this change.
Merge: fast-forward ready.
