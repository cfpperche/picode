# 2026-09-11 — accept-adr-0119

Owner accepted ADR-0119 (package-config-descriptors) in conversation.

- Flipped the ADR's Status to "accepted" and the index row in
  `docs/decisions/README.md` to match.
- `docs/handoff.md` item 3 now records the acceptance; the remaining work
  is unchanged (upstream `picode.config` proposal, docs-site entry).
- No code change; no changelog fragment (governance edit, not
  user-visible behavior).

Gates: `make close` green on `feat/accept-adr-0119`; fast-forward to
`main` and `make ci` from the root.
