# 2026-09-21 — feat/omp-skip: the last ubuntu failure was one row needing a vendor binary

One commit. `TestGuestLaneCommandsMatchTheEngineByteForByte` was the only thing between ubuntu and a green matrix, and it failed for the same reason `TestInboxFlagParsing` did: the machine it was written on has the binary.

Almost every row in that table is pure argv construction. Omp's removal is the exception — it asks the engine whether the target is one of its `extensions`, and the engine answers by running `omp config get`. The row now declares `needsBin` and `requireCLIBinary` skips it where the vendor binary is absent, the shape `internal/tmux`'s `requireTmux` already uses. Where omp is installed the row still runs, so nothing is traded for a green runner.

Reproduced both ways by putting omp on and off PATH — a restricted-PATH directory is a cheap way to be the runner for a minute, and the whole package plus `internal/clipkgs` are green under it.

Verified: `make close` green, scoped. visual-review: n/a. Nothing deployed.

## Next up

- macOS still holds eight: three devserver tests (failing before any of this), `TestGitWatchTickWorktreeAliasUnderSecondSpelling`, two `TestNativeObservation*`, and `TestStopIdleFencesConversationAndCommands` — which passed on ubuntu one run and failed on macOS the next, so it is a flake and not a macOS fact. Three different sessions own that set.
