# 2026-09-24 — agy-trust

The owner asked for the Antigravity measurement the probes left open.
Interactive runs in a tmux on a private socket, the `ls` approved by hand
(no settings change): in an exactly trusted start folder Antigravity 1.2.10
has GEMINI.md and AGENTS.md from there up to the repository root at the
first turn, cut at 24,000 bytes. The earlier "none at start" came from
headless `-p` (never loads them) and from the session in which the folder
was first trusted.

- `agyTrusted` reads `trustedWorkspaces` (exact folder, like Grok);
  untrusted cells and an `agy-untrusted` finding; the cut is back.
- The `feat-instr-probes` changelog fragment is corrected in place.
