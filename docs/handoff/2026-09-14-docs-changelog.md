# 2026-09-14 — docs-changelog: public Changelog on VitePress

Shipped: `/changelog` generated at `make docs` from `CHANGELOG.md` plus
fragments (Unreleased included). Not committed. Nav + Reference. Vue
`<tag>` in old entries escaped. Vale skips the generated file.

Verified: tests; `make docs` + Vale. agent-browser :4180 — desktop
light/dark, phone 390×844. Screenshots `var/screenshots/docs-changelog-*.png`
(not committed).

visual-review: n/a (docs site).

Merge: fast-forward ready.
