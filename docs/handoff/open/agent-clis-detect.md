# Detect-only Agent CLIs (Muse Code, Antigravity)

## Next

- Antigravity update-check needs a read-only probe. `agy update` mutates; do not wire it until a `--check` or manifest URL is verified.

## Debts

- Antigravity (`agy`) has no update-check in PiCode yet.
- Launch, sessions, messages and intercept for Muse Code / Antigravity are out of scope until a later slice.
