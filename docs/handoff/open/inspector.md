# Inspector

## Debts

- Inspector: the Files filter covers loaded rows only; This-agent chips need the agent's tab; `gh pr view` answers cache a minute.
- [ ] Inspector Changes: cross-repo session-touched following still future work (needs per-CLI session parsing).
- [ ] Inspector Changes: nested non-linked clones invisible to `git worktree list`, so never followed.
- [x] Pre-existing: git-menu command-hint truncation at 260px rail width. (Paid: every hint carries the exact prepared command as a hover title.)
- [x] Session Changes truncation line says "worktrees" with no tooltip (jargon audit: terminal-averse copy wants a plain word or a one-line tooltip). (Paid: plain "copies of the project" copy with the real term in the hover title.)
