# Desktop shell drag & drop

## Debts

- [x] Confirm on a real Windows machine that the rebuilt shell (with `disable_drag_drop_handler`) delivers HTML5 drag events: editor-tab reorder both directions, titlebar drag still moves the window, and dropping a file onto the composer still works. (Owner-confirmed 2026-09-17: tab reorder works in the restarted shell.)
