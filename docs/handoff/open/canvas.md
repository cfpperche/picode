# Canvas

## Next

- Drawings need the owner's choice: sketch inside a panel, or free strokes on the plane (a second object layer, own selection/z-order).
- v1.1 (`docs/plans/matrix-app.md` §5 phase 5): quick reply, frozen frames, sidebar drag, tile badge. C5 needs its own plan.

## Debts

- File and diff panels are door-less since the toolbar change (2026-09-12): a tool must first say what a rectangle means for an open tab.
- C2–C4 (2026-09-10): `+`/`-`/`0` need a focused panel; a still ages only when the feed moved.

## Notes

- Anchors (2026-09-11): the link preview cannot show the spread, so a crowded drop shifts the line one gap.
- Edges (ADR-0116): no keyboard path to *draw* a link — the Messages audit list reads and revokes.
