# Desktop v2 shell

## Next

- Desktop v2 (ADR-0120): the frame contract landed (ADR-0121) — the served UI claims the frame via `data-picode-frame`, the injected fallback retires itself; the UI side goes live at the owner deploy. Next: Phase 3 CDP browser. Plan: `docs/plans/desktop-v2.md`.

## Debts

- Desktop asks for `/desktop/favicon.svg` and gets 404.
