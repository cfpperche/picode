# 2026-09-21 — docs-shots-leak: a failed docs-shots run leaves nothing behind
Shipped: commit 12c78d40 (6 files, +206/−19) — a failed `docs-shots` run left behind everything it had started, because two exits skipped their cleanup. `scripts/docs-shots.mjs`: `main()`'s `catch` called `process.exit(1)`, which skips the `finally { release?.() }`, so the owned fixture daemon stayed alive; it now sets `process.exitCode = 1`, and the fixture release and the browser-session close both run in `main`'s `finally`. The run's one named browser session moved from `run()` to `main()` (so its lifetime has one owner) and `run()` lost its dead `release` parameter. `cmd/picode-docs-fixture/main.go`: the fixture now ends itself on SIGINT/SIGTERM — `shutdown(dir, m)` kills its private tmux server (`tmux.Manager.KillServer`) and only then removes its data dir (unlinking a live socket hides the server instead of ending it); installed right after the tmux manager is built, so a kill during boot cleans up too. `internal/tmux/tmux.go`: new `Manager.KillServer` (ends the server that Manager's own socket names; "nothing to kill" is fine) plus one shared `refuseUserServer` guard — an empty socket, anything inside the user's socket directory, and the socket `$TMUX` names (`socketFromTmuxEnv`) — now used by both kill paths.
Verified: measured pre-fix on this machine — seven leaked `/tmp/picode-docs-shots-fixture` daemons, thirty stale `picode-docs-fixture-*` dirs, and a failed run on the browser path leaked thirteen chrome processes.
Both leaks reproduced pre-fix, deterministically: a run throwing right after the fixture spawns → fixtures 7→8; a run throwing after the browser session opened → chromes 11→24. Same two runs post-fix: fixtures 7→7, dirs 30→30, chromes 11→11, exit code still 1.
One full harness run (it fails at the first surface here because this worktree's UI is not built) also left nothing behind: dirs 30→30, tmux servers on fixture sockets 43→43.
The real kill path with a **live** private server: fixture started, a terminal created through its API (session `picode-sh-shell-7f5e07`), `hub stop` (SIGTERM) → exit 0, process gone, its tmux server gone, that session gone everywhere, its data dir gone.
Tests: `cmd/picode-docs-fixture` `TestShutdownEndsTheServerBeforeTheDirectory` observes the pane's own PID (`Kill(pid,0)` until ESRCH) and fails against the pre-fix cleanup ("the private server leaked"), passes after; `internal/tmux` `TestKillServerEndsTheManagersOwnServer` (including a second call on an ended server) and `TestKillServerRefusesTheUsersServer` (empty socket / the user's dir / the `$TMUX` socket), the latter proven to fail when the guard is stubbed out.
Gates: `make ci-scoped` PASS (fmt, vet, hooks, go[8], test-js, docs), `make close` PASS.
Blind spot: the harness's *success* path was not observed green end-to-end in this worktree (no built UI here ⇒ the first surface fails first); on success the session close is the same call, relocated into `main`'s finally, and the fixture release is unchanged.
visual-review: n/a — no user-facing surface changed.
Not done / debts: no changelog fragment — dev-harness hygiene, not user-visible. The `docs-shots` fixture-leak bullet in `docs/handoff/open/captures.md` is flipped to `[x]` (paid, record kept); the other open item there (the `app-mobile-inbox` flake) is untouched and still open; nothing new was filed.
Merge: not fast-forward ready — main moved; `git merge main` in this worktree, rerun `make close`, then fast-forward from the root.
