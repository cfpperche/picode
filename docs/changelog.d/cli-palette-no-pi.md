### Changed

- **Command palette: CLI commands no longer assume Pi.** With no agent
  selected, CLI settings, Packages, Skills, Connectors and Providers open the
  Agent CLIs catalog so you pick the CLI, instead of opening Pi's page. With
  an agent selected they open that agent's CLI, as before. Mobile More's
  search shortcuts follow the same rule.
