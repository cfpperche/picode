### Fixed

- **Claude usage shows again after you sign in to Claude Code.** The
  Anthropic account PiCode reads usage with now follows Claude Code's own
  login, so signing in again (in PiCode or inside Claude Code) replaces the
  old login instead of leaving "sign in again" on screen. PiCode also stops
  renewing that login itself, which could sign Claude Code out.
