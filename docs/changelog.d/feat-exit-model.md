### Fixed

- **Outcomes name the model of every agent.** A removed Claude Code, Codex,
  Omp or Muse Code agent's record now says which model it ran — the one its
  sessions used most — instead of leaving the model blank.
