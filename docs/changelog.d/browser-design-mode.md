### Added

- Design mode in the browser surface: click an element on the live page, describe the change, and the pick (cropped screenshot + viewport coordinates + intent) goes to the agent as context. The agent inspects the element with its own browser tools and edits the code.
