### Added

- **Tables look like tables in the Live view.** Markdown tables in a file
  opened in Live now show as a formatted table — header row, borders, column
  alignment, and bold, code and links inside cells. Click a cell to edit its
  text; the table returns to its formatted look when you move away. A wide
  table scrolls sideways on its own.
