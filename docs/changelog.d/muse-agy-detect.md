### Added
- **Muse Code and Antigravity in Agent CLIs.** The catalog lists Meta's `muse`
  and Google's `agy`: installed or not, Check setup, and — for Muse Code —
  Check for updates against its release channel. Launch, messages and the
  rest of the CLI panes stay off for these two.

### Changed
- Agent CLIs can list a CLI for detection only, without turning on Activity
  reporting or New terminal.
