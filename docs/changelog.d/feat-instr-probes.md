### Changed

- **Instructions: Muse Code, Antigravity and Omp measured.** Muse Code's
  rules are confirmed by a run. Antigravity reads `GEMINI.md` and
  `AGENTS.md` only in a folder you trusted in it — that exact folder — and
  the page now says when a folder is not trusted. Omp's
  `.agent/AGENTS.md` now wins a tie with `.agents/AGENTS.md`, as Omp does.
