### Changed

- Agent CLIs: the desktop rail button now follows the context rule of the CLI panes (ADR-0179) — with an agent selected it opens the view directly at that agent's CLI instead of defaulting to Pi; with no agent in context the legacy `#/clis` address is kept.
