### Added
- **Agent CLIs: quick launch settings.** The launch editor for Pi, Claude Code, Codex and OpenCode offers model, approvals, reasoning/thinking and sandbox controls above the advanced argument fields, with a one-line warning on dangerous picks.

### Changed
- **Agent CLIs: launch profiles open by default** once a CLI has at least one profile, instead of hiding behind a collapsed section.
