### Fixed

- **Package configuration pages keep your unsaved edits when you switch
  layers.** On a page like pi-roles, switching between the workspace and
  the agent layer dropped the draft you had not saved yet; each layer now
  keeps its own until you save or discard it.
- **Phone: the Connectors Marketplace toolbar is aligned.** The layer
  switcher and the button beside it share one height when the row wraps.
