### Added

- **Snippets.** Save a reusable prompt under Tools or the command palette
  (`#/snippets`). Placeholders use `{{name}}`; the studio previews the
  expanded text. Sending it to an agent or a terminal comes next.
