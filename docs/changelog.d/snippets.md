### Added

- **Snippets.** Save a reusable prompt under Tools or the command palette
  (`#/snippets`). Placeholders use `{{name}}`. Type `/snip:` in an agent
  composer to insert or send one, pick **Send snippet** in the command
  palette, or right-click a running Agent CLI terminal and choose
  **Send to terminal…** Command snippets come next.
