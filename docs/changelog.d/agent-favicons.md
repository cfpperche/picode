### Fixed

- **Sidebar: agent CLI favicons now match the tab strip in both themes.** The
  white chip that dressed Claude, Codex, Grok, OpenCode and Hermes marks on
  the dark sidebar is gone — every CLI favicon renders full bleed and
  recolors with the theme (light marks on dark, dark marks on light), the
  same art the tab strip wears. The collapsed workspace face strip separates
  its marks with a small gap instead of overlapping them into a smudge.
