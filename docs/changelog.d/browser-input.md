### Added

- Browser surface control: the Watch-only chip is now a toggle. With control on, clicks, typing and touch on the browser view drive the agent's browser; consent lives in the session (`/browser-input` in a TUI, or the toggle itself) and resets on new sessions. (ADR-0115)
