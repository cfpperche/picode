### Fixed

- **Pi wears its official mark again.** The Pi favicon had drifted into a
  neutral grey re-ink; it is pi.dev's own art at the official ink now —
  near-black on the light theme, near-white on the dark one — everywhere a
  CLI favicon renders (sidebar, tab strip, notices, the Agent CLIs catalog,
  the phone's Work rows).
