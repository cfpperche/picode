### Changed

- **One login per CLI.** Using an account in a CLI when another CLI already
  holds that same login now offers a separate sign-in instead: sharing one
  login lets the first CLI to renew it sign the other out. A login two CLIs
  already share is marked "shared with …" in Providers.
