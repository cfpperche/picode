# Design references

Owner-provided screenshots of benchmark products. **Drop images here** —
they become the ground truth for vision-capable review sessions
(`.pi/skills/visual-review`) against `benchmark-visual-anatomy.md`.

Naming: `<product>-<view>.png` (e.g. `cursor-composer.png`,
`t3code-main.png`, `paseo-terminal.png`).
